# SMAR-2663 Test Cases

## Approval Metadata

| Field | Value |
|---|---|
| Ticket | SMAR-2663 |
| Status | Approved |
| Approved Source | Current Jira SMAR-2663 and comments 44495/44510; Confluence page 2626715650 v8; direct Second Brain update command |
| Approved On | 2026-08-07 |
| Approved Scope | Two Staging API cases covering unauthenticated and valid-authenticated access to the removed route |
| Validation State | Request/response capture and a protected authentication control are tester-accessible evidence requirements |

## Test Case Coverage Summary

| Group | Coverage Objective | Key Scenarios | Priority Focus | Status |
|---|---|---|---|---|
| Group 1 | Prove unauthenticated callers cannot retrieve the former device listing | No credentials; exact 404; no data; no redirect | High | Approved |
| Group 2 | Prove valid authentication cannot restore the removed route | Valid supported credential; exact 404; no data | High | Approved |

## Approved Case Ledger

| Group | Approved IDs | Added | Updated | Merged/Removed | Deferred | Last Review |
|---|---|---|---|---|---|---|
| Group 1 | `SMAR-2663-G1-01` | Stored as new ticket case | Normalized to Second Brain schema | None | Production execution | 2026-08-07 |
| Group 2 | `SMAR-2663-G2-01` | Stored as new ticket case | Normalized to Second Brain schema | None | Production execution | 2026-08-07 |

## Duplicate Review

- Existing related cases checked: SMAR-2656 and SMAR-2658.
- No duplicate requiring merge or removal was found.
- SMAR-2656 covers deprecated dispatcher request classes, while SMAR-2663 covers the separate direct `GET /api/devices` route.
- The two SMAR-2663 cases remain separate because they verify distinct unauthenticated and valid-authenticated security conditions.

## Group 1 — Unauthenticated Endpoint Protection

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2663-G1-01 | High | Staging old backend — `GET /api/devices` | GET | Verify the removed device-listing route returns 404 without authentication | • The SMAR-2663 remediation is deployed to the approved Staging legacy instance.<br>• The endpoint is reachable from the approved QA network or VPN.<br>• The API client has no stored authorization defaults or authentication cookies.<br>• Redirect following is disabled or redirect history is captured. | 1. Open a new API-client session for the approved Staging legacy host.<br>2. Clear authorization headers, authentication cookies, API keys, and inherited credentials.<br>3. Configure `GET /api/devices` without query parameters or a request body.<br>4. Capture the final outgoing method, URL, and headers.<br>5. Send the request once.<br>6. Record the response status, headers, body, and redirect history. | **Verify after step #4:**<br>• The captured request contains no authorization header, authentication cookie, API key, or other credential.<br><br>**Verify after step #5:**<br>• Exact HTTP `404 Not Found` is returned.<br>• No `2xx` response is returned.<br>• No device list, device identifier, device property, or other device information is returned.<br>• The request does not redirect to another route that exposes device data.<br><br>**Verify after step #6:**<br>• Redacted request/response evidence records the 404 outcome without authentication material, production device data, client data, or health information. | Traceability: R1, R2, R3, R5, DEC-017. Retained from approved Confluence v8 Group 1 with safe evidence and reproducibility normalization. |

### Deferred Scenarios

- Direct Production execution is deferred because Jira comment 44495 limits QA to Staging.

### Requires Test Instrumentation

- None beyond tester-accessible API request, response, and redirect capture.

### Test Data and Cleanup

- Use only the approved Staging host. The request is read-only, so no data cleanup is expected. Clear local client credentials after execution and retain only redacted evidence.

## Group 2 — Authenticated Endpoint Protection

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2663-G2-01 | High | Staging old backend — `GET /api/devices` | GET | Verify valid authentication cannot restore the removed device-listing route | • The SMAR-2663 remediation is deployed to the approved Staging legacy instance.<br>• A valid non-expired credential supported by that instance is available for an authorized synthetic test account.<br>• An approved protected control proves the credential is valid.<br>• Redirect following is disabled or redirect history is captured. | 1. Open the approved protected control for the Staging instance.<br>2. Send the control request with the valid credential and record its accepted authentication outcome without storing the credential value.<br>3. Configure `GET /api/devices` on the same instance.<br>4. Apply the same valid credential using the instance-supported authentication mechanism.<br>5. Capture the final outgoing method, URL, and authentication-header presence with its value redacted.<br>6. Send the request once.<br>7. Record the response status, headers, body, and redirect history. | **Verify after step #2:**<br>• The protected control confirms the credential is valid and non-expired; its value is absent from stored evidence.<br><br>**Verify after step #5:**<br>• The captured request shows the approved authentication mechanism is present and targets the removed Staging route.<br><br>**Verify after step #6:**<br>• Exact HTTP `404 Not Found` is returned.<br>• No `2xx` response or `401 Unauthorized` is returned.<br>• Valid authentication does not restore access to the route.<br>• No device list, device identifier, device property, or other device information is returned.<br>• The request does not redirect to another route that exposes device data.<br><br>**Verify after step #7:**<br>• Redacted request/response evidence records the authenticated 404 outcome without credential material, production device data, client data, or health information. | Traceability: R1, R2, R3, R4, R5, DEC-017. Retained from approved Confluence v8 Group 2; the exact credential type remains environment-specific as stated by the source case. |

### Deferred Scenarios

- Direct Production execution is deferred because Jira comment 44495 limits QA to Staging.

### Requires Test Instrumentation

- A tester-accessible protected control is required to prove the credential is valid before the removed-route request.

### Test Data and Cleanup

- Use a synthetic authorized Staging account. The request is read-only, so no data cleanup is expected. Never store the credential value; clear it from the API client after execution.
