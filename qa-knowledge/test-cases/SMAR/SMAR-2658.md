# SMAR-2658 — Approved Test Cases

## Approval Metadata

| Field | Value |
|---|---|
| Ticket | SMAR-2658 |
| Status | Approved |
| Approved Source | Confluence page 2596306946 v22 and exact user approval phrase |
| Approved On | 2026-07-25 |
| Approved Scope | 72 IDs across Groups 1–6; Q1-B and Q2-B incorporated; G3-09 through G3-11 retained as deferred IDs pending Q3 |
| Validation State | Exact routes/methods/status codes and dispatcher evidence remain Open Questions / Requires Test Instrumentation |

## Test Case Coverage Summary

| Group | Coverage Objective | Key Scenarios | Priority Focus | Status |
|---|---|---|---|---|
| Group 1 | Enforce OAuth-only authentication for eighteen request classes | Valid OAuth, valid HMAC-only rejection, protected reads, writes, and no partial effects | High | Approved |
| Group 2 | Preserve the three dual-authentication exceptions | OAuth path, AWS HMAC path, and existing device/mobile HMAC consumer | High | Approved |
| Group 3 | Reject missing and invalid credentials without disclosing credential validity | Missing, malformed, expired, HMAC-only, no credentials, and response consistency | High | Approved with three deferred cases |
| Group 4 | Preserve authorization and tenant isolation after authentication | Wrong role, cross-tenant, inaccessible resource, and no enumeration | High | Approved |
| Group 5 | Protect data integrity for rejected writes | Community News, file groups, access details, procedures, concierge, and address changes | High | Approved |
| Group 6 | Protect client workflows | CP Desktop, Portal Web, session expiry, permissions, and cross-platform consistency | High | Approved |

## Approved Case Ledger

| Group | Approved IDs | Added | Updated | Merged/Removed | Deferred | Last Review |
|---|---|---|---|---|---|---|
| Group 1 | `SMAR-2658-G1-01`–`SMAR-2658-G1-18` | None | Q2-B response-contract wording | None | None | 2026-07-25 |
| Group 2 | `SMAR-2658-G2-01`–`SMAR-2658-G2-07` | None | Q2-B response-contract wording | None | None | 2026-07-25 |
| Group 3 | `SMAR-2658-G3-01`–`SMAR-2658-G3-14` | None | Q2-B response-contract wording | None | `G3-09`, `G3-10`, `G3-11` pending Q3 | 2026-07-25 |
| Group 4 | `SMAR-2658-G4-01`–`SMAR-2658-G4-08` | None | Q2-B response-contract wording | None | None | 2026-07-25 |
| Group 5 | `SMAR-2658-G5-01`–`SMAR-2658-G5-11` | None | Q2-B response-contract wording | None | None | 2026-07-25 |
| Group 6 | `SMAR-2658-G6-01`–`SMAR-2658-G6-14` | None | Q1-B platform scope | None | None | 2026-07-25 |

## Duplicate Review

- Existing related test cases checked: Yes.
- Duplicate coverage found: No duplicate requiring removal.
- Group 1 proves the authentication boundary per request class; Group 5 separately proves persistent and downstream integrity for rejected writes.
- Group 5’s publication and update cases are retained because each checks a distinct workflow or persistence risk.
- Group 6 is retained because client session, UI permission, and cross-platform behaviour cannot be proven by dispatcher API cases alone.
- Decision: Keep all 72 IDs; defer only the three unresolved mixed-credential cases.

## Evidence Reviewed

- Jira SMAR-2658 current description, request-class matrix, acceptance intent, and developer note.
- Confluence page 2596306946 v22 containing the reviewed 72-case suite.
- Confluence API Responses (Old BE) page 2123300865 v2.
- HMAC Hash Authentication Usage page 2573598721.
- User decisions Q1-B and Q2-B, plus the exact approval phrase dated 2026-07-25.

## Could Not Verify

- Exact deployed endpoint route, HTTP method, dispatcher mapping, and request body for each request class.
- Exact numeric authentication-failure status and precise unauthorized/error payload fields.
- Runtime logs, persistence queries, audit records, queue/event evidence, and approved correlation identifiers.
- Mixed valid/invalid credential precedence for the three dual-authentication exceptions.

## QA Assumptions

- `{{domain}}/api/dispatcher` and `POST` are retained from the approved source page only as execution placeholders until implementation mapping is verified.
- Existing OAuth authorization, tenant checks, validation, and accepted response behaviour remain unchanged.
- Authentication rejection occurs before business logic and creates no protected write, audit mutation, or downstream action.

## Group 1 — OAuth-Only Request Authentication

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2658-G1-01 | High | {{domain}}/api/dispatcher - `AddCommunityNewsRequest` | POST | Verify AddCommunityNewsRequest accepts OAuth and rejects HMAC-only authentication | - Exact route and request contract are recorded.<br>- Authorized OAuth user can create Community News.<br>- Valid legacy HMAC credentials are available.<br>- Two unique valid news payloads, News A and News B, are prepared.<br>- Neither news item currently exists. | **Request 1:**<br>1. Submit News A with `Authorization: Bearer {validOAuthToken}` and no HMAC authentication.<br>**Request 2:**<br>2. Submit News B using valid HMAC authentication and no OAuth token.<br>Both payloads satisfy all existing business validations. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Response follows the existing Add Community News schema.<br>- Exactly one News A record is created.<br>- Created values, tenant and actor fields match the request and OAuth user.<br>- News A can be retrieved through an authorized request.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope is returned using the generic unauthorized schema.<br>- Response does not disclose whether the HMAC signature was valid.<br>- News B is not created.<br>- No partial Community News record, accepted-action audit entry or status change exists.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-02 | High | {{domain}}/api/dispatcher - `ArchiveCommunityNewsRequest` | POST | Verify ArchiveCommunityNewsRequest accepts OAuth and rejects HMAC-only authentication | - Two active Community News records, News A and News B, exist.<br>- Neither record is archived.<br>- OAuth user can archive both records.<br>- Valid HMAC credentials are available. | **Request 1:** Archive News A using valid OAuth only.<br>**Request 2:** Archive News B using valid HMAC only.<br>Use the existing valid identifiers and request contract. | **Verify after Request 1:**<br>- Existing accepted status and response schema are returned.<br>- News A changes to the existing archived state.<br>- Archive actor and timestamp identify the OAuth action.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- News B remains active and unarchived.<br>- Archive timestamp, actor and related audit fields remain unchanged.<br>- No protected record details beyond the generic error are returned.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-03 | High | {{domain}}/api/dispatcher - `GetCommunityNewsRequest` | POST | Verify GetCommunityNewsRequest returns data with OAuth and rejects HMAC-only authentication | - A Community News record exists and its identifier is known.<br>- OAuth user can view the record.<br>- Valid HMAC credentials are available. | **Request 1:** Retrieve the record using valid OAuth only.<br>**Request 2:** Retrieve the same record using valid HMAC only. | **Verify after Request 1:**<br>- `200 OK` or the existing accepted status is returned.<br>- Response contains the requested Community News record.<br>- Identifier, title, content, status and existing metadata match stored data.<br>- Response contract is unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No Community News title, content, identifier metadata or other protected record data is returned.<br>- Response does not reveal whether HMAC validation succeeded.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-04 | High | {{domain}}/api/dispatcher - `ListCommunityNewsRequest` | POST | Verify ListCommunityNewsRequest lists news with OAuth and rejects HMAC-only authentication | - The test tenant contains multiple Community News records with known statuses.<br>- OAuth user can list Community News.<br>- Valid HMAC credentials are available. | **Request 1:** Submit the existing list request, filters and pagination using valid OAuth only.<br>**Request 2:** Submit the identical request using valid HMAC only. | **Verify after Request 1:**<br>- `200 OK` or the existing accepted status is returned.<br>- Expected Community News records are returned according to existing filter and pagination rules.<br>- Response collection and paging schema are unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No collection, count, paging token, titles or protected news data is returned.<br>- Stored Community News records remain unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-05 | High | {{domain}}/api/dispatcher - `PublishCommunityNewsRequest` | POST | Verify PublishCommunityNewsRequest accepts OAuth and rejects HMAC-only authentication | - News A and News B exist in an unpublished state.<br>- OAuth user can publish both records.<br>- Valid HMAC credentials are available.<br>- Existing publish timestamps and statuses are recorded. | **Request 1:** Publish News A using valid OAuth only.<br>**Request 2:** Publish News B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status and response schema are returned.<br>- News A changes to the existing published state.<br>- Published timestamp and actor identify the OAuth action.<br>- News A becomes available through the existing published-news workflow.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- News B remains unpublished.<br>- Published timestamp, actor and status remain unchanged.<br>- No publish workflow or accepted-action audit record is produced for News B.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-06 | High | {{domain}}/api/dispatcher - `UpdateCommunityNewsRequest` | POST | Verify UpdateCommunityNewsRequest accepts OAuth and rejects HMAC-only authentication | - Editable News A and News B records exist with recorded original values.<br>- OAuth user can update both records.<br>- Valid HMAC credentials are available.<br>- Updates A and B satisfy existing validation. | **Request 1:** Update News A using valid OAuth only.<br>**Request 2:** Update News B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status and updated-record schema are returned.<br>- Only requested fields on News A are changed.<br>- `UpdAt` and updater fields identify the OAuth action.<br>- Re-reading News A returns the updated values.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- News B retains all original values.<br>- `UpdAt`, updater, publication state and audit history remain unchanged.<br>- No partial field update is present.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-07 | High | {{domain}}/api/dispatcher - `GetFileGroupsRequest` | POST | Verify GetFileGroupsRequest returns file groups with OAuth and rejects HMAC-only authentication | - A client file belongs to one or more known file groups.<br>- OAuth user can access the file and its groups.<br>- Valid HMAC credentials are available. | **Request 1:** Retrieve file groups using valid OAuth only.<br>**Request 2:** Submit the identical request using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Expected file-group identifiers and data are returned.<br>- Response schema and relationship data remain unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No file-group, file, tenant or relationship data is returned.<br>- Existing file-group relationships remain unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-08 | High | {{domain}}/api/dispatcher - `GetGroupAccessDetailsRequest` | POST | Verify GetGroupAccessDetailsRequest returns access details with OAuth and rejects HMAC-only authentication | - A group exists with known access-detail configuration.<br>- OAuth user can view the configuration.<br>- Valid HMAC credentials are available. | **Request 1:** Retrieve group access details using valid OAuth only.<br>**Request 2:** Retrieve the same details using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Access-detail values match stored configuration.<br>- Response schema is unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No role, permission, group membership or access configuration is exposed.<br>- Stored access configuration remains unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-09 | High | {{domain}}/api/dispatcher - `GetGroupProceduresRequest` | POST | Verify GetGroupProceduresRequest returns procedures with OAuth and rejects HMAC-only authentication | - A group has known procedure content configured.<br>- OAuth user can view the procedures.<br>- Valid HMAC credentials are available. | **Request 1:** Retrieve group procedures using valid OAuth only.<br>**Request 2:** Retrieve the same procedures using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Procedure identifiers, content, sequence and metadata match stored values.<br>- Response contract is unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No procedure content or metadata is returned.<br>- Procedure records remain unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-10 | High | {{domain}}/api/dispatcher - `GetGroupsRequest` | POST | Verify GetGroupsRequest returns groups with OAuth and rejects HMAC-only authentication | - Multiple groups exist in the authorized tenant.<br>- OAuth user can retrieve the applicable groups.<br>- Valid HMAC credentials are available. | **Request 1:** Submit the existing group retrieval request using valid OAuth only.<br>**Request 2:** Submit the identical request using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Expected group records are returned according to existing filters and permissions.<br>- Collection, count and pagination contract remain unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No group names, identifiers, tenant information or paging metadata is exposed.<br>- Group data remains unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-11 | High | {{domain}}/api/dispatcher - `UpdateFileGroupRequest` | POST | Verify UpdateFileGroupRequest accepts OAuth and rejects HMAC-only authentication | - File Group A and File Group B exist with recorded original values.<br>- OAuth user can update both.<br>- Valid HMAC credentials are available.<br>- Update payloads satisfy existing validation. | **Request 1:** Update File Group A using valid OAuth only.<br>**Request 2:** Update File Group B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status and response schema are returned.<br>- File Group A contains the requested changes.<br>- Unmodified properties and relationships remain unchanged.<br>- Update actor and timestamp identify the OAuth action.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- File Group B retains its original values and relationships.<br>- No update timestamp, actor or audit-history change is recorded.<br>- No partial relationship update occurs.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-12 | High | {{domain}}/api/dispatcher - `UpdateGroupAccessDetailsRequest` | POST | Verify UpdateGroupAccessDetailsRequest accepts OAuth and rejects HMAC-only authentication | - Group A and Group B have equivalent editable access-detail configurations.<br>- Original access rules are recorded.<br>- OAuth user can update both groups.<br>- Valid HMAC credentials are available. | **Request 1:** Update Group A access details using valid OAuth only.<br>**Request 2:** Apply an equivalent valid update to Group B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Group A access configuration matches the OAuth request.<br>- Effective access reflects the updated rules according to existing behaviour.<br>- Actor and timestamp identify the OAuth action.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- Group B access rules remain unchanged.<br>- No user gains or loses access because of the rejected request.<br>- Audit fields and history remain unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-13 | High | {{domain}}/api/dispatcher - `UpdateGroupProceduresRequest` | POST | Verify UpdateGroupProceduresRequest accepts OAuth and rejects HMAC-only authentication | - Group A and Group B contain known editable procedures.<br>- Original content and sequence are recorded.<br>- OAuth user can update both.<br>- Valid HMAC credentials are available. | **Request 1:** Update Group A procedures using valid OAuth only.<br>**Request 2:** Apply an equivalent valid update to Group B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Requested Group A procedure content and order are persisted.<br>- Unmodified procedure data remains unchanged.<br>- Audit fields identify the OAuth action.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- Group B procedure content, order and metadata remain unchanged.<br>- No partial procedure or orphan child record is created.<br>- No accepted-update audit entry exists.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-14 | High | {{domain}}/api/dispatcher - `UpdateGroupRequest` | POST | Verify UpdateGroupRequest accepts OAuth and rejects HMAC-only authentication | - Group A and Group B exist with recorded original values.<br>- OAuth user can update both groups.<br>- Valid HMAC credentials are available.<br>- Valid distinguishable updates are prepared. | **Request 1:** Update Group A using valid OAuth only.<br>**Request 2:** Update Group B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status and response schema are returned.<br>- Only requested Group A properties are changed.<br>- Group identifier, tenant and unrelated relationships remain unchanged.<br>- Actor and timestamp identify the OAuth action.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- Group B retains all original properties and relationships.<br>- Update metadata and audit history remain unchanged.<br>- No duplicate or replacement group is created.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-15 | High | {{domain}}/api/dispatcher - `ListConciergeServicesRequest` | POST | Verify ListConciergeServicesRequest lists services with OAuth and rejects HMAC-only authentication | - The authorized tenant/group contains known concierge services.<br>- OAuth user can view the services.<br>- Valid HMAC credentials are available. | **Request 1:** List concierge services using valid OAuth only.<br>**Request 2:** Submit the identical list request using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Expected concierge services are returned according to current filters and permissions.<br>- Service identifiers, status and response collection schema are unchanged.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No concierge service, provider, tenant or configuration data is returned.<br>- Existing services remain unchanged.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-16 | High | {{domain}}/api/dispatcher - `UpdateConciergeServiceRequest` | POST | Verify UpdateConciergeServiceRequest accepts OAuth and rejects HMAC-only authentication | - Concierge Service A and Service B exist with recorded original values.<br>- OAuth user can update both.<br>- Valid HMAC credentials are available.<br>- Valid update payloads are prepared. | **Request 1:** Update Service A using valid OAuth only.<br>**Request 2:** Apply an equivalent update to Service B using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status and response schema are returned.<br>- Service A contains the requested changes.<br>- Unmodified fields remain unchanged.<br>- Update actor and timestamp identify the OAuth action.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- Service B retains all original values and status.<br>- Update metadata and audit history remain unchanged.<br>- No partial or duplicate concierge record is created.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-17 | High | {{domain}}/api/dispatcher - `FindAddressesRequest` | POST | Verify FindAddressesRequest returns address matches with OAuth and rejects HMAC-only authentication | - A valid address-search term is known to return results.<br>- OAuth user can use address search.<br>- Valid HMAC credentials are available.<br>- Any external address provider required by the existing workflow is available. | **Request 1:** Search using the valid address term and OAuth only.<br>**Request 2:** Submit the identical search using valid HMAC only. | **Verify after Request 1:**<br>- Existing accepted status is returned.<br>- Expected address matches are returned using the unchanged response schema.<br>- Search formatting and result mapping follow existing behaviour.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No address matches, provider response details or protected contextual data is returned.<br>- The external address provider is not called when authentication is rejected, where this is observable through logs or mocks.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G1-18 | High | {{domain}}/api/dispatcher - `RequestClientFileAddressChangeRequest` | POST | Verify RequestClientFileAddressChangeRequest accepts OAuth and rejects HMAC-only authentication | - Client File A and Client File B are eligible for an address-change request.<br>- Neither has an existing conflicting pending request.<br>- OAuth user can submit address changes.<br>- Current addresses and workflow states are recorded.<br>- Valid HMAC credentials are available. | **Request 1:** Submit a valid address-change request for Client File A using OAuth only.<br>**Request 2:** Submit an equivalent valid address-change request for Client File B using HMAC only. | **Verify after Request 1:**<br>- Existing accepted status and response schema are returned.<br>- Exactly one address-change request is created for Client File A.<br>- Requested address, client-file reference, status, actor and timestamps match existing behaviour.<br>- The request is visible through the existing Portal/CP or authorized API workflow.<br>**Verify after Request 2:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No address-change request is created for Client File B.<br>- Client File B’s address and pending-request state remain unchanged.<br>- No approval task, event, notification, audit-accepted outcome entry or downstream address-change processing is initiated.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |

### Requires Test Instrumentation

- Confirm route/method/request contract before API execution. Use sanitized dispatcher decisions, persistence/audit evidence, and isolated downstream event evidence where the result is not observable through an approved UI or API.

### Test Data and Cleanup

- Use synthetic non-production tenants, records, tokens, and HMAC material. Isolate notification recipients and downstream integrations. Remove disposable records without deleting unrelated data or required audit evidence.

## Group 2 — Dual-Authentication Exceptions

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2658-G2-01 | High | {{domain}}/api/dispatcher - `FindDevicesPortalRequest` | POST | Verify FindDevicesPortalRequest returns authorized devices with OAuth authentication | - Exact route, method, and request contract are recorded.<br>- An authorized Portal Web/CP Desktop OAuth user exists.<br>- A client file with known devices exists.<br>- Expected device identifiers, types, statuses, and relationships are recorded.<br>- Browser/API request capture and backend correlation logs are available. | 1. Obtain a valid OAuth token for the authorized user.<br>2. Build a valid `FindDevicesPortalRequest` for the known client/file context.<br>3. Add `Authorization: Bearer {validOAuthToken}`.<br>4. Do not include HMAC authentication.<br>5. Submit the request.<br>6. Retrieve the same device list through the existing authorized Portal Web/CP Desktop workflow for comparison. | **Verify after request step 5:**<br>- `200 OK` or the existing accepted status is returned.<br>- Response follows the unchanged `FindDevicesPortalRequest` schema.<br>- Only devices belonging to the requested and authorized context are returned.<br>- Device identifiers, types, status, client-file relationships, and paging data match stored data.<br>- No authentication fallback or HMAC requirement is reported.<br>- No device, client-file, or relationship record is modified.<br>**Verify after comparison step 6:**<br>- Portal Web/CP Desktop displays the same applicable device records.<br>- API and UI values are consistent.<br>- The browser request uses OAuth and completes without a new authentication error.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G2-02 | High | {{domain}}/api/dispatcher - `FindDevicesPortalRequest` | POST | Verify FindDevicesPortalRequest remains accessible to the AWS backend with valid HMAC authentication | - Same client/file and device data as G2-01 exist.<br>- A valid HMAC credential supported by the current environment is available.<br>- An AWS backend integration path or controlled equivalent request is available.<br>- Expected device results are recorded. | 1. Submit the valid `FindDevicesPortalRequest` through the AWS backend or controlled integration request.<br>2. Include valid HMAC authentication.<br>3. Do not include an OAuth token.<br>4. Record the correlation ID and returned device data. | **Verify after request:**<br>- `200 OK` or the existing accepted status is returned.<br>- The request is not rejected for missing OAuth authentication.<br>- Response schema remains unchanged.<br>- Returned device records match the requested client/file context.<br>- Device results match the equivalent OAuth result from G2-01, subject to existing consumer-specific filtering.<br>- Exactly one response is processed for the request.<br>- No device, relationship, client-file, audit, or status data is modified.<br>- Integration logs identify accepted HMAC authentication without exposing the HMAC key or raw signature.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G2-03 | High | {{domain}}/api/dispatcher - `FindFilesPortalRequest` | POST | Verify FindFilesPortalRequest returns authorized files with OAuth authentication | - An authorized OAuth user exists.<br>- The user can access multiple known client files.<br>- Expected file identifiers, statuses, groups, and paging results are recorded.<br>- Exact request route and contract are available. | 1. Submit a valid `FindFilesPortalRequest` using OAuth only.<br>2. Use the existing filters, search parameters, and pagination applicable to the request.<br>3. Compare the API result with the existing Portal Web/CP Desktop file listing. | **Verify after request:**<br>- `200 OK` or the existing accepted status is returned.<br>- Response uses the unchanged file-search/list schema.<br>- Expected authorized files are returned according to existing filters and paging rules.<br>- File identifiers, status, client details, group relationships, and paging metadata match stored data.<br>- No HMAC credential is required.<br>- No file or relationship data is modified.<br>**Verify after UI comparison:**<br>- Portal Web/CP Desktop shows the same applicable files.<br>- Existing filtering and paging remain functional.<br>- No new authentication error is displayed.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G2-04 | High | {{domain}}/api/dispatcher - `FindFilesPortalRequest` | POST | Verify FindFilesPortalRequest remains accessible to the AWS backend with valid HMAC authentication | - Same file dataset as G2-03 exists.<br>- Valid current HMAC credentials are available.<br>- AWS integration or a controlled backend request can invoke the request.<br>- Expected file results are recorded. | 1. Submit the valid `FindFilesPortalRequest` through the AWS backend or controlled integration path.<br>2. Include valid HMAC authentication only.<br>3. Record the correlation ID, response status, and returned file list. | **Verify after request:**<br>- `200 OK` or the existing accepted status is returned.<br>- Missing OAuth does not cause rejection.<br>- Response contract remains unchanged.<br>- Returned files match the requested filters and authorized backend context.<br>- Results are consistent with the equivalent OAuth call, subject to existing consumer-specific filtering.<br>- Pagination token and result count remain valid.<br>- Exactly one request result is processed.<br>- No file, client, group, status, or audit data is changed.<br>- Logs do not expose the HMAC key or signature.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G2-05 | High | {{domain}}/api/dispatcher - `GetClientFileRequest` | POST | Verify GetClientFileRequest returns the client file with OAuth authentication | - An authorized Portal Web/CP Desktop OAuth user exists.<br>- A client file with known client, address, group, device, and status data exists.<br>- The user has existing permission to access the file.<br>- Exact route and identifier contract are recorded. | 1. Submit `GetClientFileRequest` for the known client file using OAuth only.<br>2. Do not include HMAC authentication.<br>3. Open the same client file through Portal Web/CP Desktop for comparison. | **Verify after API request:**<br>- `200 OK` or the existing accepted status is returned.<br>- Response follows the unchanged client-file schema.<br>- The requested client file is returned once.<br>- Client-file identifier, client details, status, address, group, and applicable device relationships match stored data.<br>- No HMAC credential is required.<br>- No client-file or related record is modified.<br>**Verify after UI comparison:**<br>- Portal Web/CP Desktop displays data consistent with the API response.<br>- Existing permission controls remain applied.<br>- No new authentication message or UI regression occurs.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G2-06 | High | {{domain}}/api/dispatcher - `GetClientFileRequest` | POST | Verify GetClientFileRequest remains accessible to the AWS backend with valid HMAC authentication | - Same client file as G2-05 exists.<br>- Valid current backend HMAC credentials are available.<br>- AWS backend or controlled integration can invoke the legacy request.<br>- Expected response values are recorded. | 1. Submit `GetClientFileRequest` from the AWS backend or controlled backend integration.<br>2. Include valid HMAC authentication only.<br>3. Do not include OAuth.<br>4. Capture the response and correlation ID. | **Verify after request:**<br>- `200 OK` or the existing accepted status is returned.<br>- The request is accepted without OAuth.<br>- Response contract remains unchanged.<br>- The correct client file is returned for the requested identifier.<br>- Client, file, address, group, status, and applicable device values match stored data.<br>- Result is consistent with the equivalent OAuth response, except for existing consumer-specific field filtering.<br>- Exactly one response is processed.<br>- No client-file, client, device, address, group, or audit data is modified.<br>- Logs confirm the accepted backend request without exposing authentication secrets.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G2-07 | High | {{domain}}/api/dispatcher - `GetClientFileRequest` | POST | Verify GetClientFileRequest remains accessible to an existing device or mobile consumer using HMAC authentication | - A supported device, SOS Mobile, Carer App, or IPD consumer uses `GetClientFileRequest` in the test environment.<br>- The consumer is linked to a known client file.<br>- The consumer’s current authentication remains valid.<br>- The observable workflow that loads client-file data is identified.<br>- Device/app and backend logs are available. | 1. Start the supported device or application consumer.<br>2. Perform the existing action that invokes `GetClientFileRequest`, such as loading or synchronizing the assigned client file.<br>3. Capture the outgoing request or backend correlation log.<br>4. Confirm the request uses the existing HMAC authentication path and does not provide an OAuth user token. | **Verify after the workflow:**<br>- `GetClientFileRequest` is accepted by the legacy backend.<br>- The consumer does not receive the existing Old Backend unauthorized/error response envelope because OAuth is absent.<br>- The correct assigned client-file data is returned.<br>- The app/device displays or consumes the client-file data according to existing behaviour.<br>- No unrelated client file is returned.<br>- No duplicate synchronization or repeated record is produced.<br>- No client-file or device relationship is changed solely by reading the data.<br>- Logs identify one accepted request and do not expose the HMAC key or signature.<br>- No regression occurs in the consumer’s normal startup, refresh, or synchronization workflow.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |

### Requires Test Instrumentation

- Confirm route/method/request contract before API execution. Use sanitized dispatcher decisions, persistence/audit evidence, and isolated downstream event evidence where the result is not observable through an approved UI or API.

### Test Data and Cleanup

- Use synthetic non-production tenants, records, tokens, and HMAC material. Isolate notification recipients and downstream integrations. Remove disposable records without deleting unrelated data or required audit evidence.

## Group 3 — Invalid, Missing, and Mixed Credentials

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2658-G3-01 | High | {{domain}}/api/dispatcher -  `GetCommunityNewsRequest` | POST | Verify an OAuth-only request rejects a request with no authentication credentials | - Exact route and valid record identifier are known.<br>- The record exists and is accessible with valid OAuth.<br>- Baseline accepted OAuth response has been captured. | Submit the same valid request without:<br>- OAuth bearer token.<br>- HMAC signature.<br>- Any other supported authentication credential. | - the existing Old Backend unauthorized/error response envelope is returned.<br>- Response follows the generic unauthorized schema.<br>- Response does not include Community News data, record identifiers, tenant details, or permission information.<br>- Response does not reveal which authentication mechanisms the request supports.<br>- No business-data or audit change occurs.<br>- Backend returns no unhandled exception or `500` response.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-02 | High | {{domain}}/api/dispatcher -  `GetCommunityNewsRequest` | POST | Verify an OAuth-only request rejects malformed OAuth authentication | - Same data and endpoint as G3-01.<br>- Valid request parameters are available. | Repeat the request separately with:<br>1. `Authorization: Bearer` with no token.<br>2. A non-JWT or structurally malformed token.<br>3. An unsupported authorization scheme.<br>4. A token containing invalid encoding or separators.<br>Do not include HMAC. | **Verify for every variant:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- Generic unauthorized schema is unchanged across variants.<br>- No protected record data is returned.<br>- No parser, stack trace, token-decoding detail, claim value, or internal authentication message is exposed.<br>- No `400` or `500` is returned solely because the credential is malformed, unless the existing authentication contract explicitly defines otherwise.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-03 | High | {{domain}}/api/dispatcher -  `GetCommunityNewsRequest` | POST | Verify an OAuth-only request rejects invalid OAuth even when valid HMAC is supplied | - The request succeeds independently with valid OAuth.<br>- Valid legacy HMAC credentials are available.<br>- Expired, incorrectly signed, wrong-issuer, and wrong-audience OAuth tokens are available. | Submit the valid request repeatedly using valid HMAC plus each OAuth variant:<br>1. Expired OAuth token.<br>2. Incorrectly signed token.<br>3. Wrong issuer.<br>4. Wrong audience.<br>5. Revoked token, where supported. | **Verify for every variant:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- Valid HMAC does not allow the request to succeed.<br>- HMAC is not used as a fallback after OAuth validation fails.<br>- Response uses the generic unauthorized schema.<br>- Response does not identify whether OAuth failed because of expiry, issuer, audience, revocation, or signature.<br>- No Community News data is returned.<br>- No business or audit state changes.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-04 | High | {{domain}}/api/dispatcher -  `AddCommunityNewsRequest` | POST | Verify an OAuth-only write request with valid OAuth and valid HMAC creates one record through OAuth | - Authorized OAuth user can create Community News.<br>- Valid HMAC credentials are available.<br>- A unique valid news payload is prepared.<br>- No record with the unique title/reference exists. | Submit one request containing:<br>- Valid OAuth bearer token.<br>- Valid HMAC authentication.<br>- One valid Community News payload. | - Existing accepted create status and response schema are returned.<br>- Exactly one Community News record is created.<br>- The created actor and user audit values correspond to the OAuth user.<br>- HMAC presence does not create a second authentication path or duplicate operation.<br>- Exactly one record, one accepted audit entry, and one applicable downstream action exist.<br>- Re-reading the record returns one result only.<br>- No authentication warning is returned because OAuth is valid.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-05 | High | {{domain}}/api/dispatcher -  `GetGroupsRequest` | POST | Verify an OAuth-only request succeeds with valid OAuth when an invalid HMAC signature is also supplied | - Valid authorized OAuth token exists.<br>- An intentionally invalid HMAC signature is available.<br>- Expected group results are known. | Submit the request with:<br>- Valid OAuth bearer token.<br>- Invalid HMAC authentication.<br>- Valid request filters and parameters. | - Existing accepted status is returned.<br>- Expected authorized groups are returned.<br>- Invalid HMAC does not override or invalidate the valid OAuth token.<br>- Result follows existing role, tenant, filter, and paging rules.<br>- Response does not report HMAC validation information.<br>- No duplicate backend processing occurs.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-06 | High | {{domain}}/api/dispatcher -  `FindAddressesRequest` | POST | Verify an OAuth-only request rejects blank or unsupported OAuth instead of falling back to valid HMAC | - A valid address search returns known results using OAuth.<br>- Valid HMAC credentials are available.<br>- Address provider availability is confirmed. | Submit separately with valid HMAC plus:<br>1. Blank bearer token.<br>2. Unsupported authorization scheme.<br>3. Whitespace-only token.<br>4. Corrupted bearer token. | **Verify for every variant:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- The address search is not accepted through HMAC.<br>- No address matches or provider details are returned.<br>- External address-provider processing is not initiated where observable.<br>- Response does not reveal that the HMAC credential was valid.<br>- No `500` response or unhandled authentication error occurs.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-07 | High | {{domain}}/api/dispatcher -  `UpdateGroupRequest` | POST | Verify conflicting or duplicate OAuth authorization values are rejected without updating the group | - An editable test group exists.<br>- Original field and audit values are recorded.<br>- A valid update payload is available.<br>- At least one valid and one invalid token are available. | Submit the update using an ambiguous authorization input supported by the test client, such as:<br>- Two Authorization headers.<br>- Multiple bearer values in one header.<br>- Valid and invalid bearer values supplied together.<br>Do not submit the valid token through an unambiguous single header. | - Request is rejected using the existing authentication error contract, preferably the existing Old Backend unauthorized/error response envelope.<br>- The backend does not select an arbitrary bearer value.<br>- Group values remain unchanged.<br>- `UpdAt`, `UpdBy`, permissions, relationships, and audit history remain unchanged.<br>- No partial update or duplicate request processing occurs.<br>- Response does not expose token parsing or validation details.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-08 | High | {{domain}}/api/dispatcher -  `FindDevicesPortalRequest` | POST | Verify a dual-authentication exception rejects a request when both OAuth and HMAC are absent | - Known client/file device data exists.<br>- The same request succeeds with valid OAuth and with valid HMAC.<br>- Exact request contract is recorded. | Submit the valid request without OAuth or HMAC authentication. | - the existing Old Backend unauthorized/error response envelope is returned.<br>- Generic unauthorized schema is returned.<br>- No device identifiers, types, relationships, tenant information, counts, or paging data are returned.<br>- Request is not treated as publicly accessible because it is an exception.<br>- No business data changes.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-09 | High | {{domain}}/api/dispatcher -  `FindDevicesPortalRequest` | POST | Verify FindDevicesPortalRequest accepts valid HMAC when the supplied OAuth token is invalid | - Valid HMAC credentials are available.<br>- An invalid or expired OAuth token is available.<br>- Known expected device results are recorded. | Submit the request with:<br>- Invalid or expired OAuth token.<br>- Valid HMAC authentication.<br>- Valid file/client search context. | **Open Question — mixed-credential precedence:** This case is deferred. Do not assert acceptance or rejection until BA/Product/Dev confirms the rule. QA recommends fail closed: reject the request when any supplied credential is invalid. No business or downstream result is approved for execution yet.<br>**HTTP contract control:** HTTP 200 and HTTP 401 are non-normative reference values only; capture the implemented Old Backend status and sanitized envelope after the decision. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. **Deferred — Q3 Open Question:** mixed valid/invalid credential precedence is not approved. |
| SMAR-2658-G3-10 | High | {{domain}}/api/dispatcher -  `FindFilesPortalRequest` | POST | Verify FindFilesPortalRequest accepts valid OAuth when the supplied HMAC signature is invalid | - Valid authorized OAuth token exists.<br>- Invalid HMAC credentials are available.<br>- Expected file results are recorded. | Submit the request with:<br>- Valid OAuth bearer token.<br>- Invalid HMAC authentication.<br>- Valid filters and pagination. | **Open Question — mixed-credential precedence:** This case is deferred. Do not assert acceptance or rejection until BA/Product/Dev confirms the rule. QA recommends fail closed: reject the request when any supplied credential is invalid. No business or downstream result is approved for execution yet.<br>**HTTP contract control:** HTTP 200 and HTTP 401 are non-normative reference values only; capture the implemented Old Backend status and sanitized envelope after the decision. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. **Deferred — Q3 Open Question:** mixed valid/invalid credential precedence is not approved. |
| SMAR-2658-G3-11 | High | {{domain}}/api/dispatcher -  `GetClientFileRequest` | POST | Verify GetClientFileRequest accepts valid HMAC when the supplied OAuth token is invalid | • `GetClientFileRequest` is configured as a dual-authentication exception.<br>• A known client file exists and is accessible to the supported HMAC consumer.<br>• The expected client-file response values are recorded.<br>• Valid current HMAC credentials are available.<br>• An expired, malformed, or incorrectly signed OAuth token is available.<br>• Request and backend correlation logs are accessible. | 1. Build a valid `GetClientFileRequest` for the known client file.<br>2. Include the invalid OAuth bearer token.<br>3. Include valid HMAC authentication for the exact request.<br>4. Submit the request.<br>5. Retrieve the same client file using a valid HMAC-only baseline request for comparison. | **Open Question — mixed-credential precedence:** This case is deferred. Do not assert acceptance or rejection until BA/Product/Dev confirms the rule. QA recommends fail closed: reject the request when any supplied credential is invalid. No business or downstream result is approved for execution yet.<br>**HTTP contract control:** HTTP 200 and HTTP 401 are non-normative reference values only; capture the implemented Old Backend status and sanitized envelope after the decision. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. **Deferred — Q3 Open Question:** mixed valid/invalid credential precedence is not approved. |
| SMAR-2658-G3-12 | High | Representative restricted and dual-authentication request routes | POST | Verify authentication failures return a consistent sanitized unauthorized contract across request categories | - One restricted read request is available.<br>- One restricted write request is available.<br>- One dual-authentication exception request is available.<br>- Known valid request identifiers and payloads are prepared.<br>- Invalid OAuth, invalid HMAC, and missing-authentication variants are available.<br>- Response headers, bodies, timing, logs and data state can be inspected. | Submit the following variants:<br>1. Restricted read request with valid HMAC only.<br>2. Restricted write request with valid HMAC only.<br>3. Restricted request with invalid OAuth and valid HMAC.<br>4. Exception request with both OAuth and HMAC invalid.<br>5. Restricted request without authentication.<br>6. Exception request without authentication. | **Verify for each request:**<br>- The standard unauthorized HTTP status is returned according to the confirmed platform contract.<br>- The same generic unauthorized error schema is used.<br>- Error code and message do not reveal whether OAuth or HMAC was evaluated, recognized, or structurally valid.<br>- Response does not identify whether the endpoint supports HMAC.<br>- No protected record data, resource-existence detail, tenant value, stack trace, signing detail, or validation internals are returned.<br>- Restricted write requests produce no partial persistence, timestamp update, audit-accepted outcome record, event or downstream request.<br>- Authentication failure is logged with request name and correlation ID only.<br>- Logs contain no raw bearer token, HMAC signature, API key, shared key, or full authorization header.<br>- No obvious deterministic response difference exposes the accepted or failed authentication branch.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-13 | High | {{domain}}/api/dispatcher -  `GetCommunityNewsRequest` | POST | Verify an OAuth-only read request succeeds through OAuth when valid HMAC is also supplied | - `GetCommunityNewsRequest` is configured as OAuth-only.<br>- A known Community News record exists.<br>- An OAuth user is authorized to view the record.<br>- Valid OAuth and valid HMAC credentials are available.<br>- A valid OAuth-only baseline response has been recorded. | 1. Submit `GetCommunityNewsRequest` using valid OAuth only and record the response.<br>2. Submit the identical request using both valid OAuth and valid HMAC.<br>3. Compare both responses and backend request logs. | **Verify after step 2:**<br>- Existing accepted HTTP status is returned.<br>- The requested Community News record is returned.<br>- Valid OAuth remains sufficient even though HMAC is present.<br>- HMAC presence does not cause an unauthorized response.<br>- HMAC presence does not broaden the records or fields returned.<br>- Exactly one request and one response are processed.<br>- No duplicate retrieval or fallback authentication occurs.<br>**Verify after step 3:**<br>- OAuth-only and mixed-credential responses contain equivalent business data.<br>- Record identifier, title, content, status and metadata match stored values.<br>- Logs show accepted OAuth processing without exposing whether the HMAC signature was valid.<br>- No business data is modified.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G3-14 | High | Restricted route for `GetCommunityNewsRequest`; exception route for `FindDevicesPortalRequest` | POST | Verify the same HMAC-authenticated session rejects an OAuth-only route and accepts a dual-authentication route | - Both routes and valid request parameters are recorded.<br>- A Community News record exists.<br>- A client file with known devices exists.<br>- Valid HMAC credentials can be generated for both requests.<br>- One HTTP client/session can reuse the same connection pool and session context.<br>- Request ordering and correlation IDs can be inspected. | Using the same HTTP client/session:<br>1. Submit HMAC-only `GetCommunityNewsRequest`.<br>2. Immediately submit HMAC-only `FindDevicesPortalRequest`.<br>3. Submit HMAC-only `GetCommunityNewsRequest` again.<br>4. Start a new sequence by calling the exception route first.<br>5. Immediately call the restricted route.<br>6. Compare responses and chronological logs. | **Verify after steps 1 and 3:**<br>- The restricted request returns the standard generic unauthorized response.<br>- No Community News data is returned.<br>**Verify after steps 2 and 4:**<br>- The exception request returns the existing accepted status.<br>- Only devices belonging to the requested authorized context are returned.<br>**Verify after steps 5–6:**<br>- Authentication behaviour is determined independently for each request class or route.<br>- A accepted exception request does not enable HMAC for the following restricted request.<br>- A rejected restricted request does not disable HMAC for the following exception request.<br>- No authentication decision is cached globally for the connection or session.<br>- Each request has one independent correlation ID and one response.<br>- No record is duplicated, modified, or returned under the preceding route’s authentication classification.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |

### Requires Test Instrumentation

- Confirm route/method/request contract before API execution. Use sanitized dispatcher decisions, persistence/audit evidence, and isolated downstream event evidence where the result is not observable through an approved UI or API.

### Test Data and Cleanup

- Use synthetic non-production tenants, records, tokens, and HMAC material. Isolate notification recipients and downstream integrations. Remove disposable records without deleting unrelated data or required audit evidence.

## Group 4 — Authorization and Tenant Isolation

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2658-G4-01 | High | {{domain}}/api/dispatcher -  `GetGroupsRequest` | POST | Verify an authorized OAuth user can retrieve permitted groups after OAuth-only restriction | - Exact route and request contract are recorded.<br>- User A has a valid OAuth token.<br>- User A has existing permission to view Groups A and B in Tenant A.<br>- User A does not have global administrative access.<br>- Expected group data is recorded. | 1. Submit `GetGroupsRequest` with User A’s valid OAuth token.<br>2. Use the existing Tenant A context, filters, and pagination.<br>3. Do not include HMAC authentication. | **Verify after request:**<br>- `200 OK` or the existing accepted status is returned.<br>- Response follows the unchanged groups contract.<br>- Groups A and B are returned according to existing permissions and filters.<br>- No groups outside User A’s permitted scope are returned.<br>- Existing pagination, count, and sort behaviour remain unchanged.<br>- Authentication accepted outcome does not broaden User A’s access.<br>- No group or permission data is modified.<br>- Portal Web/CP Desktop group listing remains consistent with the API response.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-02 | High | {{domain}}/api/dispatcher -  `GetGroupsRequest` | POST | Verify a valid OAuth user without group-view permission is denied access | - User B has a valid, non-expired OAuth token.<br>- User B belongs to the correct environment and tenant but lacks permission to view groups.<br>- Groups A and B exist.<br>- Existing authorization denial status and error schema are known. | Submit the same valid group request used in G4-01 with User B’s OAuth token. | **Verify after request:**<br>- Existing authorization denial status is returned, such as `403 Forbidden` or the route’s existing resource-hidden result.<br>- Response follows the existing authorization error schema.<br>- Response is not treated as an authentication failure because the token is valid.<br>- No group names, identifiers, membership, access settings, tenant information, counts, or paging data are returned.<br>- No HMAC fallback is attempted.<br>- No group, role, permission, or audit data is modified.<br>- Portal Web/CP Desktop does not expose the restricted group data.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-03 | High | {{domain}}/api/dispatcher -  `GetCommunityNewsRequest` | POST | Verify a valid OAuth user cannot retrieve Community News from another tenant | - Tenant A and Tenant B exist.<br>- User A has valid OAuth access to Tenant A only.<br>- News B belongs to Tenant B.<br>- News B’s identifier is known for controlled negative testing.<br>- User A has no cross-tenant administration permission. | Submit `GetCommunityNewsRequest` for News B using User A’s valid OAuth token and Tenant A context. | **Verify after request:**<br>- Existing cross-tenant authorization denial status is returned.<br>- Response uses the existing generic forbidden/not-found schema.<br>- News B title, body, publication state, author, tenant, identifier metadata, and timestamps are not returned.<br>- Response does not confirm whether News B exists when the existing contract hides resource existence.<br>- News B remains unchanged.<br>- No read-accepted outcome audit or tenant relationship is created.<br>- A subsequent authorized Tenant B request still returns the original News B record.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-04 | High | {{domain}}/api/dispatcher -  `GetFileGroupsRequest` | POST | Verify a valid OAuth user cannot retrieve file groups for an unauthorized client file | - User A has valid OAuth access to Client File A.<br>- User A does not have access to Client File B.<br>- Both files belong to valid configured groups.<br>- File B and its group relationships are recorded.<br>- Existing resource-level authorization behaviour is known. | 1. Submit `GetFileGroupsRequest` for Client File A using User A’s token as a positive control.<br>2. Submit the same request for Client File B using User A’s token. | **Verify after positive-control request:**<br>- Existing accepted status is returned.<br>- File A’s permitted group relationships are returned.<br>**Verify after unauthorized request:**<br>- Existing authorization denial status is returned.<br>- No File B group identifiers, names, membership, tenant data, or relationship metadata are returned.<br>- Response does not broaden access because User A has a valid OAuth token.<br>- File B and its group relationships remain unchanged.<br>- Portal Web/CP Desktop does not display File B or its groups to User A.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-05 | High | {{domain}}/api/dispatcher -  `UpdateGroupRequest` | POST | Verify a valid OAuth user cannot update a group outside their authorized scope | - Group A is editable by User A.<br>- Group B is outside User A’s authorized group or tenant scope.<br>- User A has a valid OAuth token.<br>- Original Group B values, relationships, `UpdAt`, `UpdBy`, and audit history are recorded.<br>- A valid distinguishable update payload is prepared. | 1. Submit an update to Group B using User A’s valid OAuth token.<br>2. Re-read Group B through an authorized administrator account.<br>3. Review accessible audit/history evidence. | **Verify after update request:**<br>- Existing authorization denial status is returned.<br>- Response follows the existing authorization error schema.<br>- The request is not accepted merely because OAuth authentication succeeded.<br>- No partial accepted outcome body or updated Group B object is returned.<br>**Verify after re-read and audit review:**<br>- Group B retains its original name, status, settings, relationships, and tenant ownership.<br>- `UpdAt`, `UpdBy`, and updater UUID remain unchanged.<br>- No accepted-update audit entry is created.<br>- No permission, membership, procedure, or access-detail record is modified.<br>- No queue, event, notification, or downstream integration is triggered.<br>- Retrying with an authorized administrator creates only one valid update.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-06 | High | {{domain}}/api/dispatcher -  `RequestClientFileAddressChangeRequest` | POST | Verify a valid OAuth user without address-change permission cannot start the workflow | - User A has a valid OAuth token and can view Client File A.<br>- User A lacks permission to request a client-file address change.<br>- Client File A has no conflicting pending address-change request.<br>- Current address, pending workflow state, audit fields, and notification state are recorded.<br>- A valid new-address payload is prepared. | 1. Submit `RequestClientFileAddressChangeRequest` for Client File A using User A’s OAuth token.<br>2. Re-open the client file through an authorized user.<br>3. Check the address-change request list, workflow state, notifications, and accessible audit evidence. | **Verify after request:**<br>- Existing authorization denial status is returned.<br>- Response follows the existing authorization error schema.<br>- No pending, submitted, approved, or partially created address-change request is returned.<br>**Verify after data and workflow checks:**<br>- Client File A retains its current address.<br>- No address-change request record exists.<br>- No workflow task, approval item, queue message, event, SMS, email, or notification is produced.<br>- Client-file and workflow audit fields remain unchanged.<br>- Portal Web/CP Desktop does not show a accepted submission.<br>- An authorized user can subsequently submit one valid request without encountering a duplicate created by the denied call.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-07 | High | {{domain}}/api/dispatcher -  `GetClientFileRequest` | POST | Verify GetClientFileRequest retains existing client-file authorization when called with OAuth | - `GetClientFileRequest` remains a dual-authentication exception.<br>- User A has valid OAuth access to Client File A but not Client File B.<br>- Both client files exist with known details.<br>- Existing authorization-denial behaviour is recorded. | 1. Request Client File A with User A’s valid OAuth token.<br>2. Request Client File B with the same valid OAuth token.<br>3. Do not include HMAC in either request. | **Verify after Client File A request:**<br>- Existing accepted status is returned.<br>- Correct authorized client-file data is returned.<br>**Verify after Client File B request:**<br>- Existing authorization denial status is returned.<br>- No Client File B identity, address, client, group, device, status, care, or tenant data is returned.<br>- Dual-authentication exception status does not weaken OAuth resource authorization.<br>- Neither client file nor related records are modified.<br>- Portal Web/CP Desktop access is consistent with the API result.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G4-08 | High | {{domain}}/api/dispatcher -  `UpdateGroupAccessDetailsRequest` | POST | Verify a valid OAuth user cannot grant permissions beyond their administrative authority | - User A has a valid OAuth token.<br>- User A can view the target group but cannot administer access details.<br>- Target group access rules and assigned users are recorded.<br>- A valid payload attempts to grant elevated access to another test user.<br>- Existing permission-denial contract is known. | 1. Submit `UpdateGroupAccessDetailsRequest` using User A’s valid OAuth token.<br>2. Re-read access details with an authorized administrator.<br>3. Sign in as the proposed recipient and attempt the protected group action. | **Verify after update request:**<br>- Existing authorization denial status is returned.<br>- No updated access-detail object or accepted outcome response is returned.<br>**Verify after access-detail re-read:**<br>- Original access rules and assignments remain unchanged.<br>- No user is added, removed, elevated, or downgraded.<br>- `UpdAt`, `UpdBy`, and audit history remain unchanged.<br>**Verify using the proposed recipient:**<br>- The recipient does not gain the attempted permission.<br>- Protected group functions remain unavailable according to existing rules.<br>- No partial permission cache, session, or cross-platform access change occurs.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |

### Requires Test Instrumentation

- Confirm route/method/request contract before API execution. Use sanitized dispatcher decisions, persistence/audit evidence, and isolated downstream event evidence where the result is not observable through an approved UI or API.

### Test Data and Cleanup

- Use synthetic non-production tenants, records, tokens, and HMAC material. Isolate notification recipients and downstream integrations. Remove disposable records without deleting unrelated data or required audit evidence.

## Group 5 — Rejected Write Request Data Integrity

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2658-G5-01 | High | {{domain}}/api/dispatcher -  `AddCommunityNewsRequest` | POST | Verify rejected HMAC-only Community News creation leaves no record or downstream action | - Exact route and valid request contract are recorded.<br>- Valid HMAC credentials are available.<br>- A unique Community News payload is prepared.<br>- No record with the test title/reference exists.<br>- Existing news count, audit records, queue/event state, and notification state are recorded. | 1. Submit `AddCommunityNewsRequest` using valid HMAC only.<br>2. Do not include OAuth.<br>3. Search for the unique record through an authorized API/UI request.<br>4. Review accessible audit, queue/event, notification, and request logs.<br>5. Retry the same business payload once using valid OAuth. | **Verify after rejected request:**<br>- the existing Old Backend unauthorized/error response envelope and the generic unauthorized schema are returned.<br>- No Community News identifier or accepted outcome object is returned.<br>**Verify after data checks:**<br>- No news record exists with the unique title/reference.<br>- News count remains unchanged.<br>- No draft, partial, orphan, unpublished, or hidden record is created.<br>- No accepted creation audit entry exists.<br>- No publish event, cache update, queue message, notification, or downstream action is generated.<br>**Verify after valid OAuth retry:**<br>- Exactly one Community News record is created.<br>- The record is linked to the correct tenant and OAuth actor.<br>- No duplicate originating from the rejected call exists.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-02 | High | {{domain}}/api/dispatcher - `UpdateCommunityNewsRequest`, `PublishCommunityNewsRequest`, and `ArchiveCommunityNewsRequest` | POST | Verify rejected Community News state changes leave content and lifecycle fields unchanged | - Three separate news records exist:<br>— News A for update.<br>— News B for publish.<br>— News C for archive.<br>- Original content, status, timestamps, actor fields, and audit history are recorded.<br>- Valid HMAC credentials are available. | 1. Update News A using valid HMAC only.<br>2. Publish News B using valid HMAC only.<br>3. Archive News C using valid HMAC only.<br>4. Re-read all three records using valid OAuth.<br>5. Review audit/history and applicable downstream events. | **Verify after each rejected request:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- No updated, published, or archived accepted outcome object is returned.<br>**Verify News A:**<br>- Title, body, metadata, status, `UpdAt`, and updater remain unchanged.<br>- No partial field update exists.<br>**Verify News B:**<br>- Published state, publication timestamp, publisher, and visibility remain unchanged.<br>- No publish event, cache refresh, or notification occurs.<br>**Verify News C:**<br>- Active/archive state, archive timestamp, and actor remain unchanged.<br>- News C remains available according to its original state.<br>**Verify audit/integration:**<br>- No accepted business audit entry exists for any rejected operation.<br>- No downstream message or duplicate lifecycle event exists.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-03 | High | {{domain}}/api/dispatcher -  `UpdateFileGroupRequest` | POST | Verify rejected file-group update preserves properties and file relationships | - File Group A exists with known name, status, configuration, file membership, and audit values.<br>- A valid update would change at least one property or relationship.<br>- Valid HMAC credentials are available.<br>- Current relationship count is recorded. | 1. Submit the valid file-group update using HMAC only.<br>2. Re-read File Group A using authorized OAuth.<br>3. Retrieve related files and group membership.<br>4. Review audit/history and integration logs.<br>5. Retry the update with valid OAuth. | **Verify after rejected request:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- No updated file-group response is returned.<br>**Verify after re-read:**<br>- Group properties remain identical to the recorded baseline.<br>- File membership and relationship count remain unchanged.<br>- No file is added, removed, duplicated, or orphaned.<br>- `UpdAt`, updater, and audit history remain unchanged.<br>- No relationship-sync job or event is created.<br>**Verify after OAuth retry:**<br>- The intended update is applied once.<br>- Only expected properties or relationships change.<br>- No duplicate relationship exists.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-04 | High | {{domain}}/api/dispatcher -  `UpdateGroupAccessDetailsRequest` | POST | Verify rejected group-access update does not grant or remove permissions | - A group exists with known users, roles, and access details.<br>- User B currently lacks a selected protected permission.<br>- A valid payload attempts to grant that permission.<br>- A second variation attempts to remove an existing permission from User C.<br>- Valid HMAC credentials are available. | 1. Submit the permission-grant request using valid HMAC only.<br>2. Submit the permission-removal request using valid HMAC only.<br>3. Re-read group access details using an authorized administrator.<br>4. Sign in as User B and User C and attempt the protected action.<br>5. Review audit and permission-cache evidence where accessible. | **Verify after rejected requests:**<br>- Both return the existing Old Backend unauthorized/error response envelope.<br>- No accepted outcome or updated access-detail object is returned.<br>**Verify stored access:**<br>- User B does not receive the attempted permission.<br>- User C retains the existing permission.<br>- Membership, role, access-detail records, and effective permissions remain unchanged.<br>- `UpdAt`, updater, and business audit history remain unchanged.<br>**Verify runtime access:**<br>- User B remains unable to perform the protected action.<br>- User C remains able to perform the previously permitted action.<br>- No stale or partial permission-cache state appears across Portal Web/CP Desktop sessions.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-05 | High | {{domain}}/api/dispatcher -  `UpdateGroupProceduresRequest` | POST | Verify rejected group-procedure update creates no partial or orphan procedure data | - A group has multiple procedures with known content, identifiers, sequence, and status.<br>- A valid update changes content, order, and optionally adds/removes one procedure.<br>- Valid HMAC credentials are available.<br>- Original child-record count and audit data are recorded. | 1. Submit the valid procedure update using HMAC only.<br>2. Re-read the group procedures using authorized OAuth.<br>3. Compare identifiers, content, sequence, status, and child-record count.<br>4. Review accessible history and event records.<br>5. Retry once with valid OAuth. | **Verify after rejected request:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- No updated procedure collection is returned.<br>**Verify after re-read:**<br>- Existing procedure content and sequence remain unchanged.<br>- No new child procedure is created.<br>- No existing child procedure is deleted or disabled.<br>- No orphan, duplicate, blank, or partially updated procedure exists.<br>- Child-record count remains unchanged.<br>- Audit fields and history remain unchanged.<br>- No procedure-sync event or downstream update is generated.<br>**Verify after OAuth retry:**<br>- The intended collection update occurs once.<br>- Final procedure count and sequence match the valid payload.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-06 | High | {{domain}}/api/dispatcher -  `UpdateGroupRequest` | POST | Verify rejected group update preserves properties, tenant ownership, and relationships | - Group A exists with recorded name, status, settings, tenant, relationships, and audit metadata.<br>- A valid payload changes multiple editable fields.<br>- Valid HMAC credentials are available. | 1. Submit `UpdateGroupRequest` with valid HMAC only.<br>2. Re-read the group through authorized OAuth.<br>3. Retrieve related file, user, procedure, and access-detail relationships where supported.<br>4. Review audit and integration evidence. | **Verify after rejected request:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- No updated group object is returned.<br>**Verify stored group:**<br>- All original editable properties remain unchanged.<br>- Group identifier and tenant ownership remain unchanged.<br>- File, user, procedure, and access-detail relationships remain unchanged.<br>- `UpdAt`, updater, and business history remain unchanged.<br>- No replacement or duplicate group is created.<br>- No group-sync job, cache event, or downstream message is generated.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-07 | High | {{domain}}/api/dispatcher -  `UpdateConciergeServiceRequest` | POST | Verify rejected concierge-service update produces no service or integration change | - Concierge Service A exists with known name, status, availability, provider details, and audit values.<br>- A valid update payload is prepared.<br>- Valid HMAC credentials are available.<br>- Applicable integration/export state is recorded. | 1. Submit the concierge-service update using HMAC only.<br>2. Re-read Service A using authorized OAuth.<br>3. View the service through Portal Web/CP Desktop.<br>4. Review audit, cache, event, and applicable integration/export evidence.<br>5. Retry with valid OAuth. | **Verify after rejected request:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- No updated service object is returned.<br>**Verify after API/UI checks:**<br>- Service name, status, availability, provider, and all original values remain unchanged.<br>- Portal Web/CP Desktop continues showing the original values.<br>- `UpdAt`, updater, and history remain unchanged.<br>- No duplicate or replacement service exists.<br>- No cache refresh, service event, billing/export action, notification, or unrelated integration is triggered.<br>**Verify after OAuth retry:**<br>- The intended update is applied once and visible consistently through API and UI.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-08 | High | {{domain}}/api/dispatcher -  `RequestClientFileAddressChangeRequest` | POST | Verify rejected address-change request starts no workflow, task, or notification | - Client File A is eligible for address change.<br>- No pending address-change request exists.<br>- Current address and workflow state are recorded.<br>- A valid proposed address is prepared.<br>- Valid HMAC credentials are available.<br>- Relevant workflow, queue, notification, and audit evidence is accessible. | 1. Submit the address-change request using valid HMAC only.<br>2. Re-open Client File A through authorized Portal Web/CP Desktop and API.<br>3. Search address-change request records and pending work items.<br>4. Check queues/events, tasks, notifications, SMS/email, and audit evidence.<br>5. Retry the same business request using valid OAuth. | **Verify after rejected request:**<br>- the existing Old Backend unauthorized/error response envelope is returned.<br>- No request identifier, pending status, or accepted outcome response is returned.<br>**Verify client-file state:**<br>- Current address remains unchanged.<br>- No pending, submitted, approved, rejected, or partial address-change record exists.<br>- No address history entry is created.<br>**Verify workflow/integrations:**<br>- No approval task, service request, work item, queue message, event, email, SMS, notification, or downstream address-processing call is generated.<br>- No accepted-action audit entry exists.<br>**Verify after OAuth retry:**<br>- Exactly one valid address-change request is created.<br>- Correct client-file reference, proposed address, actor, timestamp, and initial status are stored.<br>- No duplicate from the rejected attempt exists.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-09 | High | Representative restricted write request | POST | Verify concurrent HMAC rejection and OAuth retry produce one accepted business result | - A restricted create or update request is selected, preferably `AddCommunityNewsRequest` or `RequestClientFileAddressChangeRequest`.<br>- Unique test payload exists.<br>- Valid OAuth and valid HMAC credentials are available.<br>- No matching business record currently exists.<br>- Requests can be issued concurrently or nearly simultaneously. | 1. Submit the same business request using HMAC only.<br>2. At the same time or immediately after, submit the request using valid OAuth.<br>3. Wait for applicable synchronous and asynchronous processing.<br>4. Query resulting data, audit records, queues/events, and notifications. | **Verify after both requests:**<br>- HMAC request returns the existing Old Backend unauthorized/error response envelope.<br>- OAuth request returns the existing accepted result.<br>- Exactly one business record or update exists.<br>- Actor and audit fields identify only the OAuth action.<br>- No partial state from the HMAC request is merged into the valid result.<br>- Exactly one applicable event, queue message, workflow item, or notification exists.<br>- No duplicate, orphan, conflicting status, or second audit-accepted outcome entry exists.<br>- Final API and UI states are consistent.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-10 | Medium | {{domain}}/api/dispatcher -  `PublishCommunityNewsRequest` | POST | Verify rejected HMAC-only publishing does not publish Community News or trigger downstream processing | - An unpublished Community News record exists.<br>- Original status, publication timestamp and audit fields are recorded.<br>- Valid HMAC credentials are available.<br>- Publish-event, cache, notification or downstream logs are accessible where applicable. | 1. Submit `PublishCommunityNewsRequest` using valid HMAC only.<br>2. Retrieve the Community News record through an authorized OAuth request.<br>3. Inspect applicable event, queue, cache, notification and audit evidence. | **Verify after step 1:**<br>- Standard unauthorized response is returned.<br>**Verify after step 2:**<br>- News remains unpublished.<br>- Publication timestamp, actor and status remain unchanged.<br>**Verify after step 3:**<br>- No publish event, cache refresh, subscriber update, notification or accepted publish audit entry is generated.<br>- No partial or delayed publication occurs.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |
| SMAR-2658-G5-11 | High | {{domain}}/api/dispatcher -  `UpdateGroupAccessDetailsRequest` | POST | Verify rejected HMAC-only access updates do not change effective group permissions | - A test group has known access rules.<br>- Test User A currently has access.<br>- Test User B currently does not have access.<br>- Existing access details, audit fields and effective access are recorded.<br>- A valid update payload would reverse or materially change access.<br>- Valid HMAC credentials are available. | 1. Submit the valid access-change payload using HMAC only.<br>2. Retrieve Group Access Details using authorized OAuth.<br>3. Log in or query access as Test User A and Test User B.<br>4. Inspect audit/history and applicable cache or authorization-refresh evidence. | **Verify after step 1:**<br>- Standard unauthorized response is returned.<br>**Verify after step 2:**<br>- Stored group access details remain unchanged.<br>- Update timestamp and updater remain unchanged.<br>**Verify after step 3:**<br>- Test User A retains existing access.<br>- Test User B does not gain access.<br>- No permission is added, removed, or partially applied.<br>**Verify after step 4:**<br>- No accepted update audit entry or permission-refresh event is generated.<br>- No stale or delayed permission change appears.<br>**HTTP contract control (Q2-B):** Use the existing Old Backend endpoint-specific status and sanitized response envelope. HTTP 200 and HTTP 401 are non-normative reference values only; do not hardcode either value without verified implementation evidence. Authentication failures must use the current unauthorized/error contract and must not expose credential details. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. |

### Requires Test Instrumentation

- Confirm route/method/request contract before API execution. Use sanitized dispatcher decisions, persistence/audit evidence, and isolated downstream event evidence where the result is not observable through an approved UI or API.

### Test Data and Cleanup

- Use synthetic non-production tenants, records, tokens, and HMAC material. Isolate notification recipients and downstream integrations. Remove disposable records without deleting unrelated data or required audit evidence.

## Group 6 — Client Workflow Regression — CP Desktop and Portal Web

| TC ID | Priority | Test Area | Title | Preconditions | Test Steps | Expected Result | Expected Integration | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2658-G6-01 | High | CP Desktop — Community News Create | Verify CP Desktop creates Community News through OAuth without HMAC dependency | - CP Desktop is confirmed to invoke `AddCommunityNewsRequest`.<br>- Exact CP Desktop navigation path is recorded.<br>- User A has permission to create Community News.<br>- User A has a valid OAuth session.<br>- A unique valid news title and content are prepared.<br>- No matching record exists.<br>- Proxy or backend request logging is enabled. | 1. Sign in to CP Desktop as User A.<br>2. Navigate to the confirmed Community News screen.<br>3. Click Add or Create Community News.<br>4. Enter the unique title and valid content.<br>5. Complete all existing required fields.<br>6. Save the news item.<br>7. Capture the request mapped to `AddCommunityNewsRequest`.<br>8. Return to the Community News list.<br>9. Search for and open the created item. | **Verify after steps 1–3:**<br>- Existing Community News controls are available according to User A’s permissions.<br>- No new authentication or HMAC-related UI is displayed.<br>**Verify after step 6:**<br>- Existing accepted outcome confirmation or navigation occurs.<br>- CP Desktop does not display `401`, an unexpected logout, or a generic server error.<br>**Verify after step 7:**<br>- One request is sent to the mapped legacy route.<br>- The request contains the supported OAuth credential.<br>- No HMAC credential is required or generated.<br>- The backend returns the existing accepted response.<br>**Verify after steps 8–9:**<br>- Exactly one Community News item exists.<br>- Title, content, tenant, creator, status, and timestamps follow existing behaviour.<br>- The record remains available after reopen.<br>- No duplicate or partial record exists. | **Verify after step #9:** - `AddCommunityNewsRequest` accepts CP Desktop OAuth traffic.<br>- UI and stored data are consistent.<br>- Existing audit fields identify User A.<br>- No unintended notification, alarm, task, billing action, or unrelated queue event occurs. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-02 | High | CP Desktop — Community News Lifecycle | Verify CP Desktop Community News lifecycle actions continue through OAuth | - CP Desktop is confirmed to invoke the update, publish, and archive request classes.<br>- News A exists and is editable.<br>- News B exists and is unpublished.<br>- News C exists and is active/published.<br>- User A has the required permissions.<br>- Original content, statuses, and audit values are recorded.<br>- Proxy or backend logging is enabled. | 1. Open News A, change distinguishable content, and save.<br>2. Publish News B.<br>3. Archive News C.<br>4. Capture requests mapped to `UpdateCommunityNewsRequest`, `PublishCommunityNewsRequest`, and `ArchiveCommunityNewsRequest`.<br>5. Reopen all three records. | **Verify after step 1:**<br>- News A is updated once.<br>- Only requested fields change.<br>**Verify after step 2:**<br>- News B enters the existing published state.<br>- Publication actor and timestamp are recorded.<br>**Verify after step 3:**<br>- News C enters the existing archived state.<br>- Archive actor and timestamp are recorded.<br>**Verify after step 4:**<br>- Every request uses OAuth.<br>- No request uses or retries with HMAC.<br>- No unexpected authentication error occurs.<br>**Verify after step 5:**<br>- All final states persist after reopen.<br>- No duplicate update, publication, archive action, or audit entry exists. | **Verify after step #5:** - Existing audit/history and publication visibility behaviour remain unchanged.<br>- Applicable cache or publish processing occurs once.<br>- No unrelated notification or workflow is triggered. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-03 | High | CP Desktop — Groups and File Groups | Verify CP Desktop retrieves groups and file-group relationships through OAuth | - CP Desktop is confirmed to invoke `GetGroupsRequest` and `GetFileGroupsRequest`.<br>- User A can view known Groups A and B.<br>- A known client file has recorded file-group relationships.<br>- OAuth session and proxy/backend logging are active. | 1. Open the confirmed Groups screen.<br>2. Load the group list.<br>3. Open Group A.<br>4. Open the applicable file-group or grouped-client-file section.<br>5. Capture the mapped requests.<br>6. Compare the displayed values with authorized backend data. | **Verify after steps 1–4:**<br>- Expected groups and file-group relationships are displayed.<br>- Existing loading, filtering, paging, and empty-state behaviour remain unchanged.<br>- No unauthorized or cross-tenant group is shown.<br>**Verify after step 5:**<br>- Both requests use OAuth.<br>- No HMAC authentication is sent or required.<br>- Existing accepted response contracts are returned.<br>**Verify after step 6:**<br>- Group names, identifiers, statuses, memberships, and relationship counts match backend data.<br>- Read operations do not modify group or file data. | **Verify after step #6:** - `GetGroupsRequest` and `GetFileGroupsRequest` remain compatible with CP Desktop OAuth.<br>- UI/API authorization filtering remains consistent.<br>- No sync, queue, notification, or audit mutation occurs from reading data. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-04 | High | CP Desktop — Group Configuration | Verify CP Desktop updates group configuration and child settings through OAuth | - CP Desktop is confirmed to invoke the applicable group update requests.<br>- User A can update Group A.<br>- Original group values, access details, and procedures are recorded.<br>- Valid distinguishable updates are prepared.<br>- OAuth session and request logging are active. | 1. Open Group A.<br>2. Change one standard group property and save.<br>3. Change one permitted access-detail value and save.<br>4. Change one procedure field or sequence and save.<br>5. Capture the mapped update requests.<br>6. Close and reopen Group A. | **Verify after steps 2–4:**<br>- Each save completes using existing CP Desktop behaviour.<br>- Only intended values change.<br>- No new authentication message appears.<br>**Verify after step 5:**<br>- Applicable `UpdateGroupRequest`, `UpdateGroupAccessDetailsRequest`, and `UpdateGroupProceduresRequest` calls use OAuth.<br>- No call depends on HMAC.<br>- Each operation is accepted once.<br>**Verify after step 6:**<br>- Group property, access details, and procedure updates persist.<br>- Unmodified fields and relationships remain unchanged.<br>- Effective permissions reflect only the intended access change.<br>- No duplicate, orphan, or partial child record exists.<br>- Audit actor and timestamps identify User A. | **Verify after step #6:** - Group, access-detail, and procedure APIs remain consistent.<br>- Existing permission cache behaviour remains unchanged.<br>- No unrelated client, task, alarm, billing, or notification action occurs. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-05 | Medium | CP Desktop — Concierge Services | Verify CP Desktop concierge service list and update continue through OAuth | - CP Desktop is confirmed to invoke `ListConciergeServicesRequest` and/or `UpdateConciergeServiceRequest`.<br>- User A can view and update Service A.<br>- Existing service values are recorded.<br>- OAuth session and request logging are active. | 1. Open the confirmed Concierge Services screen.<br>2. Load the service list.<br>3. Open Service A.<br>4. Change one editable value and save.<br>5. Capture the list and update requests.<br>6. Reopen Service A. | **Verify after steps 1–3:**<br>- Expected services are displayed according to existing filters and permissions.<br>**Verify after step 4:**<br>- Existing accepted outcome behaviour occurs.<br>- No authentication warning or unexpected logout appears.<br>**Verify after step 5:**<br>- Applicable requests use OAuth.<br>- HMAC is not required or generated.<br>- Existing response contracts remain unchanged.<br>**Verify after step 6:**<br>- Updated value persists.<br>- Unmodified values, provider, status, and tenant remain unchanged.<br>- One audit update identifies User A.<br>- No duplicate concierge service exists. | **Verify after step #6:** - UI and API values remain consistent.<br>- Existing cache or integration behaviour occurs once where applicable.<br>- No unintended billing export, notification, task, alarm, or unrelated event is generated. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-06 | High | CP Desktop — Client Files and Devices | Verify CP Desktop loads files, client-file details, and devices through OAuth | - CP Desktop is confirmed to invoke one or more of `FindFilesPortalRequest`, `GetClientFileRequest`, and `FindDevicesPortalRequest`.<br>- User A can access Client File A and its known devices.<br>- Expected client-file and device values are recorded.<br>- OAuth session and request logging are active. | 1. Open the existing Client File list or search screen.<br>2. Find Client File A.<br>3. Open Client File A.<br>4. Expand or open the device section.<br>5. Capture all mapped legacy requests.<br>6. Compare CP Desktop values with authorized backend data. | **Verify after steps 1–4:**<br>- File listing, client-file detail, and applicable device data load successfully.<br>- Existing navigation, search, paging, and loading behaviour remain unchanged.<br>- No unauthorized client or device is displayed.<br>**Verify after step 5:**<br>- Every CP Desktop request uses OAuth.<br>- CP Desktop does not use HMAC merely because the three routes remain dual-authentication exceptions.<br>- Existing accepted response contracts are returned.<br>**Verify after step 6:**<br>- File identifier, client details, status, address, groups, devices, device types, and relationships match backend data.<br>- No read operation modifies client, file, device, relationship, or audit data.<br>- No duplicate refresh or synchronization occurs. | **Verify after step #6:** - OAuth remains the CP Desktop authentication method.<br>- HMAC compatibility remains available only for AWS/device consumers.<br>- Existing authorization filtering remains enforced. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-07 | High | CP Desktop — OAuth Session Lifecycle | Verify CP Desktop does not fall back to HMAC when OAuth expires | - CP Desktop is confirmed to use OAuth for an affected restricted request.<br>- A representative read and safe write workflow are available.<br>- OAuth expiry or revocation can be controlled.<br>- Existing CP Desktop session-renewal behaviour is documented.<br>- Request and backend logs are available. | 1. Sign in and open the representative affected screen.<br>2. Expire or revoke the OAuth token using the approved method.<br>3. Perform the affected read action.<br>4. Perform the safe affected write action.<br>5. Capture all request attempts.<br>6. Complete the existing sign-in or token-renewal flow.<br>7. Retry the action once. | **Verify after steps 3–4:**<br>- Invalid OAuth requests do not succeed.<br>- Protected data is not returned.<br>- No partial write, audit-accepted outcome entry, queue message, or notification is produced.<br>**Verify after step 5:**<br>- CP Desktop does not retry using HMAC.<br>- No uncontrolled retry loop or repeated error request occurs.<br>**Verify after step 6:**<br>- Existing login or token-renewal behaviour is shown.<br>- No new SMAR-2658-specific UI message is introduced.<br>**Verify after step 7:**<br>- The action succeeds once with renewed OAuth.<br>- Final UI, API, and stored state are consistent.<br>- No duplicate operation exists. | **Verify after step #7:** - OAuth provider/session behaviour remains unchanged.<br>- Legacy backend accepts only the renewed valid OAuth request.<br>- Rejected attempts trigger no downstream business processing. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-08 | High | Portal — Community News | Verify Portal Community News workflows continue through OAuth | - Portal is confirmed to expose Community News and invoke applicable request classes.<br>- User P has the required permissions.<br>- OAuth session is valid.<br>- Appropriate create, editable, unpublished, and published records exist.<br>- Browser developer tools are open. | 1. Sign in to Portal as User P.<br>2. Navigate to the confirmed Community News screen.<br>3. Perform the supported create, update, publish, or archive actions.<br>4. Capture each affected request.<br>5. Refresh and reopen the affected records. | **Verify after steps 2–3:**<br>- Existing Portal controls and accepted outcome behaviour remain unchanged.<br>- No authentication warning or unexpected logout occurs.<br>**Verify after step 4:**<br>- Every browser request contains OAuth authentication.<br>- Portal does not generate or retry using HMAC.<br>- Existing accepted response contracts are returned.<br>**Verify after step 5:**<br>- Each supported action persists once.<br>- Final states, actor, timestamps, and audit history follow existing behaviour.<br>- No duplicate or partial record exists. | **Verify after step #5:** - Applicable Community News request classes accept Portal OAuth.<br>- Existing publication/cache behaviour occurs once.<br>- No unrelated downstream action is triggered. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-09 | High | Portal — Groups and File Groups | Verify Portal group and file-group workflows continue through OAuth | - Portal is confirmed to invoke applicable group request classes.<br>- User P can view the configured groups and file-group relationships.<br>- If editing is supported, User P has the required update permission.<br>- OAuth session and browser capture are active. | 1. Open the confirmed Groups screen.<br>2. Load the group list.<br>3. Open a known group and related file-group data.<br>4. Perform one supported group, access-detail, or procedure update where available.<br>5. Capture all affected requests.<br>6. Refresh and reopen the data. | **Verify after steps 1–3:**<br>- Expected groups and relationships are displayed according to existing permissions.<br>- Existing filtering, paging, and loading behaviour remain unchanged.<br>**Verify after step 4:**<br>- Supported update completes once.<br>- Only intended fields change.<br>**Verify after step 5:**<br>- All requests use OAuth.<br>- No request uses HMAC or retries using HMAC.<br>- Existing response contracts remain unchanged.<br>**Verify after step 6:**<br>- Final data persist and match backend records.<br>- No duplicate, orphan, or partial configuration exists. | **Verify after step #6:** - Group-related API, UI, permissions, and audit values remain consistent.<br>- Read operations create no mutation.<br>- Updates trigger only existing applicable cache/sync behaviour. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-10 | Medium | Portal — Concierge Services | Verify Portal concierge service workflows continue through OAuth | - Portal is confirmed to invoke applicable concierge request classes.<br>- User P can view Service A.<br>- If update is supported, User P can edit Service A.<br>- Existing values are recorded.<br>- OAuth session and browser capture are active. | 1. Open Concierge Services.<br>2. Load the service list.<br>3. Open Service A.<br>4. Change one editable value if supported and save.<br>5. Capture the applicable requests.<br>6. Refresh and reopen Service A. | **Verify after steps 1–3:**<br>- Expected services are displayed according to existing permissions.<br>**Verify after step 4:**<br>- Supported update completes using existing Portal behaviour.<br>**Verify after step 5:**<br>- List and update requests use OAuth.<br>- No HMAC credential or fallback is present.<br>- Existing response contracts remain unchanged.<br>**Verify after step 6:**<br>- Values remain consistent between Portal and API.<br>- One update audit entry exists where an update occurred.<br>- No duplicate service exists. | **Verify after step #6:** - Existing cache or integration processing occurs once where applicable.<br>- No unintended billing, notification, task, or alarm is generated. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-11 | High | Portal Web → Clients → Edit Client → Property | Verify Portal address search returns the known matching address through OAuth | • Portal user has permission to view and edit the test client.<br>• A test client exists.<br>• A stable known address is prepared with expected street number, street name, suburb, state and postcode.<br>• The address provider is available or a controlled provider response is configured.<br>• Browser developer tools or an approved network proxy is available.<br>• The mapped request is confirmed as `FindAddressesRequest`. | 1. Log in to Portal Web as the authorized user.<br>2. Navigate to **Clients**.<br>3. Search for and open the test client.<br>4. Click **Edit Client**.<br>5. Open the **Property** step.<br>6. Click **Submit Address Change**.<br>7. In the Change Address Request dialog, enter the known street number and street name.<br>8. Review the returned address matches.<br>9. Select the result matching the prepared address.<br>10. Inspect the browser request and response for the address search. | **Verify after step 7:**<br>• Address search starts without an authentication or CORS error.<br>**Verify after step 8:**<br>• At least one result matches the submitted street number and street name.<br>• A result displays the expected suburb, state and postcode.<br>• No blank, duplicated, malformed, or unrelated result is selected automatically.<br>• Exact result count is required only when the provider response is controlled.<br>**Verify after step 9:**<br>• The selected result populates the address fields with the expected street number, street name, suburb, state and postcode.<br>• No address-change request is submitted merely by selecting the result.<br>**Verify after step 10:**<br>• `FindAddressesRequest` returns the existing accepted status.<br>• Request includes valid OAuth authentication.<br>• Request does not include or depend on HMAC authentication.<br>• Response data matches the address options displayed by Portal. | **Verify after step #10:** - Address provider is called only after accepted authentication.<br>- Existing workflow task, queue/event, and notification behaviour occurs exactly once where defined.<br>- No false alarm, billing, or unrelated task is generated. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-12 | High | Portal Web → Clients → Edit Client → Property | Verify Portal submits one client address-change request through OAuth | • Same Portal user and test client as G6-11.<br>• Client has no conflicting pending address-change request.<br>• Current client address is recorded.<br>• A valid alternate test address is available.<br>• Existing address-change reason and required-field rules are known.<br>• Browser network inspection is available.<br>• The mapped request is confirmed as `RequestClientFileAddressChangeRequest`. | 1. Log in to Portal Web as the authorized user.<br>2. Navigate to **Clients**.<br>3. Search for and open the test client.<br>4. Click **Edit Client**.<br>5. Open the **Property** step.<br>6. Click **Submit Address Change**.<br>7. Search for and select the prepared alternate address.<br>8. Complete the required reason or supporting fields.<br>9. Click the final submit action.<br>10. Observe the Portal result.<br>11. Inspect the generated network request.<br>12. Refresh or reopen the client and review the existing address-change status/history. | **Verify after step 9:**<br>• Submit action is processed once.<br>• Duplicate clicks are prevented or do not create duplicate requests.<br>**Verify after step 10:**<br>• Existing accepted outcome confirmation is displayed.<br>• No authentication, HMAC, or generic backend error is shown.<br>**Verify after step 11:**<br>• `RequestClientFileAddressChangeRequest` uses valid OAuth authentication.<br>• HMAC is not required or used as a fallback.<br>• Existing accepted response schema is returned.<br>• Request contains the correct client-file identifier and selected address.<br>**Verify after step 12:**<br>• Exactly one address-change request exists.<br>• Selected street, suburb, state and postcode match the submitted address.<br>• Request actor and timestamps identify the Portal user and submission time.<br>• Request status follows the existing address-change workflow.<br>• The current address is not incorrectly or partially overwritten before the existing approval/completion process.<br>• Data remains consistent after refresh or reopen. | **Verify after step #12:** - OAuth remains the Portal authentication method.<br>- HMAC remains reserved for the supported AWS/device consumers.<br>- Existing resource and tenant authorization remains enforced. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-13 | High | Portal — OAuth Session Lifecycle | Verify Portal does not fall back to HMAC when OAuth expires | - Portal uses OAuth for an affected restricted request.<br>- A representative read and safe write workflow are available.<br>- Token expiry or revocation can be controlled.<br>- Existing refresh/login behaviour is documented.<br>- Browser and backend logs are available. | 1. Sign in and open the representative screen.<br>2. Expire or revoke the OAuth token.<br>3. Perform the affected read action.<br>4. Perform the safe affected write action.<br>5. Capture all browser retries.<br>6. Complete the existing token refresh or login flow.<br>7. Retry the action once. | **Verify after steps 3–4:**<br>- Invalid OAuth requests do not return protected data or persist the write.<br>- No accepted audit entry, queue event, workflow, or notification occurs.<br>**Verify after step 5:**<br>- Portal does not retry using HMAC.<br>- No uncontrolled request loop occurs.<br>- No raw token is exposed in the UI or logs.<br>**Verify after step 6:**<br>- Existing refresh, redirect, or login behaviour occurs.<br>- No new SMAR-2658-specific error is introduced.<br>**Verify after step 7:**<br>- The action succeeds once with renewed OAuth.<br>- Final Portal, API, and stored state are consistent.<br>- No duplicate action exists. | **Verify after step #7:** - OAuth provider/session behaviour remains unchanged.<br>- Legacy backend accepts only the renewed valid request.<br>- Rejected requests create no downstream business action. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |
| SMAR-2658-G6-14 | Medium | CP Desktop / Portal — Data Consistency | Verify an OAuth-authenticated change remains consistent between CP Desktop and Portal | - CP Desktop and Portal both expose the selected record type.<br>- The platform that owns the edit action is confirmed.<br>- Both clients invoke affected SMAR-2658 requests using OAuth.<br>- User A and User P have appropriate permissions.<br>- Original values and audit fields are recorded.<br>- Request logging is enabled for both clients. | 1. Sign in to the editing platform.<br>2. Open the shared record.<br>3. Change one distinguishable supported value and save.<br>4. Capture the affected request and authentication evidence.<br>5. Reopen the record in the editing platform.<br>6. Open the same record in the other platform.<br>7. Compare both clients with authorized backend data. | **Verify after step 3:**<br>- One update succeeds using OAuth.<br>- No HMAC credential is required.<br>**Verify after step 4:**<br>- Request maps to the expected affected request class.<br>- One accepted backend operation and audit entry exist.<br>**Verify after steps 5–7:**<br>- Both clients display the same final value.<br>- API and stored data match both clients.<br>- No stale, duplicate, partial, or conflicting record exists.<br>- Audit actor and timestamp identify the editing user.<br>- Neither client uses HMAC to retrieve the updated record.<br>- No extra queue event, workflow, or notification occurs. | **Verify after step #7:** - Cross-client synchronization remains consistent.<br>- Existing cache invalidation or refresh behaviour occurs once.<br>- Authentication changes do not create platform-specific versions of the record. | Approved Confluence page 2596306946 v22; approved ID retained. Q2-B applied: existing Old Backend response contract replaces hardcoded 401 expectations. Exact route, method, numeric status, and runtime evidence require implementation confirmation. Q1-B applied: client regression scope is CP Desktop and Portal Web. |

### Requires Test Instrumentation

- Confirm route/method/request contract before API execution. Use sanitized dispatcher decisions, persistence/audit evidence, and isolated downstream event evidence where the result is not observable through an approved UI or API.

### Test Data and Cleanup

- Use synthetic non-production tenants, records, tokens, and HMAC material. Isolate notification recipients and downstream integrations. Remove disposable records without deleting unrelated data or required audit evidence.
