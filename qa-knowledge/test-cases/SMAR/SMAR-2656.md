# SMAR-2656 Test Cases

## Approval Metadata

| Field | Value |
|---|---|
| Ticket | SMAR-2656 |
| Status | Approved |
| Approved Source | Current Jira SMAR-2656; Confluence page 2609119233; exact approval phrase |
| Approved On | 2026-07-29 |
| Approved Scope | 17 cases across core deprecation, SHM/SHM ALM alarm parity, and supported-request regression |
| Validation State | Exact endpoint mappings, standard 404 body fields, dispatch traces, and active-consumer evidence remain open instrumentation dependencies |

## Test Case Coverage Summary

| Group | Coverage Objective | Key Scenarios | Priority Focus | Status |
|---|---|---|---|---|
| Group 1 | Reject every Matrix M1 request before handler execution | 33 requests; HMAC, OAuth, missing/invalid/both credentials; invalid business data; aliases | High | Approved |
| Group 2 | Enforce alarm-request deprecation in both services | Escalate and Take in SHM and SHM ALM; invalid and combined credentials | High | Approved |
| Group 3 | Preserve supported requests and current workflows | Active client/file, alarm, validation, application, restart, and multi-instance regression | High / Medium | Approved |

## Approved Case Ledger

| Group | Approved IDs | Added | Updated | Merged/Removed | Deferred | Last Review |
|---|---|---|---|---|---|---|
| Group 1 | `SMAR-2656-G1-01`–`SMAR-2656-G1-08` | None | Normalized to Second Brain schema | None | None | 2026-07-29 |
| Group 2 | `SMAR-2656-G2-01`–`SMAR-2656-G2-04` | None | Normalized to Second Brain schema | None | None | 2026-07-29 |
| Group 3 | `SMAR-2656-G3-01`–`SMAR-2656-G3-05` | None | Normalized to Second Brain schema | None | None | 2026-07-29 |

## Duplicate Review

- Existing related cases checked: SMAR-2638 and SMAR-2658.
- No duplicate requiring removal was found.
- SMAR-2638 preserves valid HMAC and removes Basic Access for two active requests; SMAR-2658 enforces OAuth-only access for active request classes. SMAR-2656 instead rejects 33 deprecated classes with exact 404.
- Group 1 covers all-request authentication and dispatch precedence; Group 2 is retained for explicit two-service alarm parity and safety-state integrity; Group 3 protects active request classes and current consumers.

## Group 1 — Core Deprecation and Authentication Rejection

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2656-G1-01 | High | Approved legacy SHM route for each Matrix M1 request | POST | Verify every deprecated SHM request returns 404 before handler execution with valid HMAC | • SMAR-2656 is deployed to STAG.<br>• An approved mapping defines the exact route, method, headers, parameters, and valid body for every M1 request.<br>• Valid synthetic records exist.<br>• Valid HMAC is proven against a supported non-deprecated control.<br>• Standard 404 schema and dispatch traces are accessible. | 1. Open the approved API collection and select M1-01.<br>2. Confirm the request mapping and synthetic data.<br>3. Send the supported non-deprecated HMAC control and record its accepted result.<br>4. Generate valid HMAC for the selected M1 request.<br>5. Send the request with a unique correlation ID.<br>6. Record status, body, headers, correlation ID, and duration.<br>7. Review SHM traces for dispatch and handler evidence.<br>8. Review applicable business state for mutation.<br>9. Repeat steps 2–8 for M1-02 through M1-33. | **Verify after step #3:**<br>• The control is not rejected with the SMAR-2656 `404 Not Found` contract, proving the HMAC setup is usable.<br><br>**Verify after step #5:**<br>• Every M1 request returns exact HTTP `404 Not Found`.<br>• The approved not-found code and message are returned.<br>• No handler-specific accepted, validation, conflict, or resource response is returned.<br>• No protected data, stack trace, internal namespace, authentication material, full payload, or health information is disclosed.<br><br>**Verify after step #7:**<br>• Trace evidence identifies deprecated-request dispatch rejection.<br>• No deprecated handler-start or handler-complete event exists.<br><br>**Verify after step #8:**<br>• No create, update, delete, alarm, notification, registration, measurement, task, calendar, file-group, or device mutation exists.<br><br>**Verify after step #9:**<br>• One recorded result exists for each of the 33 M1 requests. | Developer/operations-supported evidence: endpoint mapping, standard 404 schema, dispatch traces, and state inspection. |
| SMAR-2656-G1-02 | High | Approved OAuth-capable legacy SHM routes from Matrix M1 | POST | Verify OAuth-capable deprecated SHM requests return 404 with valid OAuth | • OAuth-capable M1 subset is documented.<br>• A valid unexpired OAuth bearer value is proven against a supported non-deprecated OAuth control.<br>• Valid business requests and dispatch traces are available. | 1. Select an OAuth-capable M1 request from the approved mapping.<br>2. Send the supported OAuth control and record its accepted result.<br>3. Remove HMAC and add valid OAuth to the M1 request.<br>4. Send the M1 request with a unique correlation ID.<br>5. Record response and trace evidence.<br>6. Repeat for every OAuth-capable M1 request.<br>7. Record non-capable requests as `N/A` with mapping evidence. | **Verify after step #2:**<br>• The control is not rejected with SMAR-2656 `404 Not Found`.<br><br>**Verify after step #4:**<br>• Every applicable M1 request returns exact HTTP `404 Not Found` using the approved schema.<br>• Valid OAuth does not override deprecated status.<br>• No requested business data or handler-specific response is returned.<br><br>**Verify after step #5:**<br>• Dispatch rejection is recorded before handler execution.<br><br>**Verify after step #7:**<br>• Each `N/A` result includes evidence that the route cannot enter OAuth dispatch; no unexecuted applicable request is marked passed. | Developer-supported endpoint applicability and traces are required. |
| SMAR-2656-G1-03 | High | Approved legacy SHM route for each Matrix M1 request | POST | Verify deprecated SHM requests return the standard 404 response without authentication | • Valid mapped M1 requests exist.<br>• The API client is configured not to add authentication automatically.<br>• Standard 404 schema and trace access are available. | 1. Select a mapped M1 request.<br>2. Confirm valid route and business data.<br>3. Remove HMAC, OAuth, authentication cookies, and other credential material.<br>4. Send the request with a unique correlation ID.<br>5. Record the response and inspect dispatch traces.<br>6. Repeat for all 33 M1 requests. | **Verify after step #4:**<br>• Exact HTTP `404 Not Found` and the approved schema are returned.<br>• No authentication challenge, business validation, protected data, resource-existence detail, redirect, `200`, `202`, or `204` response is returned.<br><br>**Verify after step #5:**<br>• No deprecated handler executes.<br>• Missing authentication does not produce a more informative response than valid-auth deprecation.<br><br>**Verify after step #6:**<br>• All 33 requests have recorded outcomes. | Authentication omission must be verified in the captured outgoing request. |
| SMAR-2656-G1-04 | High | Approved legacy SHM route for each Matrix M1 request | POST | Verify deprecated SHM requests return 404 with invalid HMAC variants | • Valid mapped M1 requests exist.<br>• Wrong-key, payload-modified, and malformed HMAC variants are prepared.<br>• Response and dispatch evidence are accessible. | 1. Select a mapped M1 request.<br>2. Send it with HMAC generated using the wrong key.<br>3. Sign a valid payload, modify the payload, and send it.<br>4. Send it with a truncated or malformed HMAC value.<br>5. Record each response and correlation ID.<br>6. Review traces for handler execution.<br>7. Repeat the three variants for all 33 M1 requests. | **Verify after steps #2–4:**<br>• Every variant returns exact HTTP `404 Not Found` using the approved schema.<br>• No request returns `500`, times out, redirects, or reaches a handler-specific response.<br>• The response does not distinguish wrong key, changed body, length, or formatting and does not echo submitted or expected HMAC material.<br><br>**Verify after step #6:**<br>• No deprecated handler-start or handler-complete event exists.<br><br>**Verify after step #7:**<br>• Every M1 request has results for all three invalid-HMAC partitions. | Uses synthetic non-production request data and redacted evidence. |
| SMAR-2656-G1-05 | High | Approved OAuth-capable legacy SHM routes from Matrix M1 | POST | Verify deprecated SHM requests return 404 with invalid OAuth variants | • OAuth-capable M1 subset is documented.<br>• Expired, invalid-signature, wrong-audience/issuer, and malformed OAuth variants are available.<br>• Dispatch trace access is available. | 1. Select an OAuth-capable M1 request.<br>2. Send it with an expired OAuth value.<br>3. Send it with an invalid-signature OAuth value.<br>4. Send it with a wrong-audience or issuer OAuth value.<br>5. Send it with a malformed bearer value.<br>6. Record responses and inspect traces.<br>7. Repeat for each applicable M1 request.<br>8. Mark non-capable requests `N/A` with mapping evidence. | **Verify after steps #2–5:**<br>• Every applicable variant returns exact HTTP `404 Not Found` using the approved schema.<br>• No variant returns `500`, gateway timeout, identity-provider details, claims, signing details, or protected data.<br><br>**Verify after step #6:**<br>• No deprecated handler executes.<br><br>**Verify after step #8:**<br>• Every `N/A` result has technical applicability evidence. | Do not store plaintext authentication material in the Second Brain or evidence. |
| SMAR-2656-G1-06 | High | Approved dual-credential-capable legacy SHM routes from Matrix M1 | POST | Verify valid HMAC and OAuth together cannot bypass deprecated-request rejection | • Applicable M1 routes capable of receiving both credential forms are documented.<br>• Each credential independently passes its supported control.<br>• Valid mapped business requests and traces are available. | 1. Select an applicable M1 request.<br>2. Prove valid HMAC and OAuth independently using supported controls.<br>3. Add both valid credential types to the M1 request.<br>4. Send the request with a unique correlation ID.<br>5. Record response and trace evidence.<br>6. Repeat for every applicable M1 request.<br>7. Record non-applicable routes as `N/A` with mapping evidence. | **Verify after step #4:**<br>• Exact HTTP `404 Not Found` and the approved schema are returned.<br>• Authentication precedence does not select a credential and execute the deprecated handler.<br>• No redirect, fallback, or alternate dispatch path is invoked.<br><br>**Verify after step #5:**<br>• No deprecated handler-start or handler-complete event exists.<br><br>**Verify after step #7:**<br>• Each `N/A` result includes technical evidence. | Distinct from single-auth cases because it verifies authentication precedence cannot bypass deprecation. |
| SMAR-2656-G1-07 | High | Approved legacy SHM route for each Matrix M1 request | POST | Verify deprecated-request rejection precedes handler-level business validation | • Each request type and route can be resolved.<br>• Valid HMAC is available.<br>• One syntactically valid but semantically invalid payload is prepared per M1 request.<br>• State and trace evidence are accessible. | 1. Select a mapped M1 request.<br>2. Prepare one applicable business error such as a non-existing formatted identifier, missing handler-required field, unsupported enum, or out-of-range value.<br>3. Confirm the transport still identifies the request type.<br>4. Send the request with valid HMAC and a unique correlation ID.<br>5. Record the response and inspect traces.<br>6. Review applicable state for partial mutation.<br>7. Repeat for all 33 M1 requests. | **Verify after step #4:**<br>• Exact HTTP `404 Not Found` is returned instead of a handler-level `400`, `409`, or domain error.<br>• The standard schema does not reveal identifier existence or invalid business fields.<br><br>**Verify after step #5:**<br>• Deprecated dispatch rejection occurs before business validator or handler execution.<br><br>**Verify after step #6:**<br>• No partial persistence or downstream business action exists.<br><br>**Verify after step #7:**<br>• Every M1 request has one recorded business-invalid partition. | Completely malformed transport that prevents request identification is excluded from this case. |
| SMAR-2656-G1-08 | High | Approved legacy SHM dispatcher request discriminator | POST | Verify supported request-name representations cannot bypass deprecated-request rejection | • Dev documents the discriminator location and representations accepted by the serializer/dispatcher.<br>• Known casing, fully qualified name, alias, or serializer casing variants are available where supported.<br>• Valid HMAC and traces are available. | 1. Select an M1 request with a documented alternate representation.<br>2. Send the canonical representation.<br>3. Send each documented resolvable casing, namespace, alias, or serializer variation.<br>4. Send one unrecognized representation.<br>5. Record responses and canonical-type traces.<br>6. Repeat for every M1 request with supported variations.<br>7. Mark absent variation types `N/A` with technical evidence. | **Verify after steps #2–3:**<br>• Every representation resolving to a deprecated class returns exact HTTP `404 Not Found` using the approved schema.<br>• No representation routes to the deprecated handler or returns business data.<br><br>**Verify after step #4:**<br>• The unrecognized representation follows the platform's existing unknown-request error contract and executes no handler.<br><br>**Verify after step #5:**<br>• Logs identify the canonical deprecated type and no active alias path.<br><br>**Verify after step #7:**<br>• No variation is invented or marked passed without applicability evidence. | Requires developer-supported discriminator and alias evidence. |

### Deferred Scenarios

- Additional request classes described as already deprecated remain pending until Dev provides an audited inventory.

### Requires Test Instrumentation

- Approved mapping for all 33 request contracts, standard 404 schema, correlation identifiers, dispatch traces, handler-entry evidence, and applicable state inspection.

### Test Data and Cleanup

- Use synthetic STAG records. Restore disposable state without removing unrelated records or required trace/audit evidence. Do not use real credentials or protected health information.

## Group 2 — Alarm Request Deprecation Across SHM and SHM ALM

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2656-G2-01 | High | SHM and SHM ALM mapped routes for `EscalateAlarmRequest` | POST | Verify EscalateAlarmRequest returns the same 404 response in SHM and SHM ALM | • SMAR-2656 is deployed to both services and build versions are recorded.<br>• Equivalent synthetic active alarms exist in an escalation-eligible state.<br>• Valid HMAC passes a supported control.<br>• Alarm baseline and trace evidence are accessible. | 1. Open the approved alarm API collection.<br>2. Navigate to the SHM `EscalateAlarmRequest` route.<br>3. Select the prepared SHM alarm and record ID, status, level, timestamp, and history count.<br>4. Send a valid HMAC request with a correlation ID.<br>5. Review response, SHM trace, and alarm state.<br>6. Navigate to the SHM ALM equivalent route.<br>7. Select the equivalent alarm and record the same baseline.<br>8. Send the equivalent valid HMAC request with a correlation ID.<br>9. Review response, SHM ALM trace, and alarm state.<br>10. Compare both service results. | **Verify after step #4:**<br>• SHM returns exact HTTP `404 Not Found` with the approved schema.<br><br>**Verify after step #5:**<br>• No escalation handler executes; status, escalation level, timestamp, and history count remain unchanged.<br>• No escalation queue, worker, notification, or provider action is created in accessible evidence.<br><br>**Verify after step #8:**<br>• SHM ALM returns exact HTTP `404 Not Found` with the approved schema.<br><br>**Verify after step #9:**<br>• No SHM ALM handler executes and its alarm baseline remains unchanged.<br><br>**Verify after step #10:**<br>• Both services return equivalent status, error code, and security meaning without exposing alarm existence, internal types, authentication material, or health information. | Use isolated alarms and recipients; cleanup must preserve required history/trace evidence. |
| SMAR-2656-G2-02 | High | SHM and SHM ALM mapped routes for `TakeAlarmRequest` | POST | Verify TakeAlarmRequest returns the same 404 response in SHM and SHM ALM | • Equivalent synthetic untaken alarms exist.<br>• No operator or worker will take them during execution.<br>• Valid HMAC, baseline, and trace evidence are available. | 1. Record SHM alarm status, assigned actor, taken timestamp, updated timestamp, and history count.<br>2. Send a valid HMAC `TakeAlarmRequest` to SHM.<br>3. Review response, trace, and alarm state.<br>4. Record the equivalent SHM ALM baseline.<br>5. Send the equivalent valid HMAC request to SHM ALM.<br>6. Review response, trace, and alarm state.<br>7. Compare both service results. | **Verify after step #2:**<br>• SHM returns exact HTTP `404 Not Found` with the approved schema.<br><br>**Verify after step #3:**<br>• No take handler executes; assigned actor, taken timestamp, status, updated timestamp, and history count remain unchanged.<br><br>**Verify after step #5:**<br>• SHM ALM returns exact HTTP `404 Not Found` with the approved schema.<br><br>**Verify after step #6:**<br>• No SHM ALM handler executes and no alarm is assigned or taken.<br><br>**Verify after step #7:**<br>• Both services return equivalent error contracts without revealing alarm existence or actor permissions. | No real alarm or operational user may be used. |
| SMAR-2656-G2-03 | High | SHM ALM mapped routes for both deprecated alarm requests | POST | Verify missing and invalid authentication cannot bypass alarm deprecation in SHM ALM | • Valid bodies exist for both alarm requests.<br>• No-auth, wrong-key, payload-modified, malformed-HMAC, and applicable invalid-OAuth variants are available.<br>• SHM baseline results and SHM ALM traces are accessible. | 1. Select `EscalateAlarmRequest` in SHM ALM.<br>2. Send it with no authentication and each invalid HMAC variant.<br>3. Send each applicable invalid OAuth variant.<br>4. Repeat steps 1–3 for `TakeAlarmRequest`.<br>5. Record every response and correlation ID.<br>6. Review traces and alarm baselines.<br>7. Compare with corresponding SHM evidence. | **Verify after steps #2–4:**<br>• Every executed variant returns exact HTTP `404 Not Found`.<br>• No variant returns `500`, timeout, redirect, handler-specific response, or credential-validation details.<br><br>**Verify after step #6:**<br>• No alarm handler executes and no alarm state/history changes.<br><br>**Verify after step #7:**<br>• SHM ALM behaviour is consistent with SHM; non-applicable OAuth variants are `N/A` only with technical evidence. | Isolated alarm data and redacted authentication evidence are required. |
| SMAR-2656-G2-04 | High | SHM and SHM ALM mapped routes for both deprecated alarm requests | POST | Verify combined valid HMAC and OAuth cannot bypass alarm deprecation | • Both credential types pass supported controls.<br>• Applicable service routes can receive both forms.<br>• Equivalent synthetic alarms, baselines, and traces are available. | 1. Select one applicable alarm request/service combination.<br>2. Add valid HMAC and valid OAuth to the same request.<br>3. Send the request with a correlation ID.<br>4. Record response, trace, and alarm state.<br>5. Repeat for every applicable Escalate/Take and SHM/SHM ALM combination.<br>6. Record non-applicable combinations as `N/A` with technical evidence. | **Verify after step #3:**<br>• Exact HTTP `404 Not Found` and the approved schema are returned.<br>• Authentication precedence, fallback, alternate routing, or cross-service forwarding does not execute the alarm command.<br><br>**Verify after step #4:**<br>• No deprecated alarm handler executes and no alarm state/history changes.<br><br>**Verify after step #6:**<br>• Every non-applicable combination has technical evidence. | Distinct safety coverage for dual-auth precedence across both services. |

### Deferred Scenarios

- None for the confirmed four alarm service/request combinations.

### Requires Test Instrumentation

- SHM and SHM ALM build versions, exact routes, correlation traces, alarm baseline/history evidence, and isolated queue/worker/notification evidence where applicable.

### Test Data and Cleanup

- Use equivalent isolated STAG alarms and non-production actors. Reset disposable state without deleting unrelated alarms or required history evidence.

## Group 3 — Supported Request and Current Workflow Regression

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2656-G3-01 | High | Approved active `FindFilesRequest` or Carer App file route | POST | Verify the supported Carer App file workflow remains available after deprecation changes | • A Carer App test user has access to a recorded set of synthetic files.<br>• Current supported authentication and an active request mapping are available.<br>• Network and backend correlation evidence are accessible. | 1. Open the supported Carer App build and sign in as the test user.<br>2. Navigate to the client/file list.<br>3. Record the expected authorized files.<br>4. Trigger file-list refresh and capture the active request and correlation ID.<br>5. Review the API response and displayed file list.<br>6. Sign out, sign in, and refresh again.<br>7. Compare UI, API, and backend evidence. | **Verify after step #4:**<br>• The active request is not rejected with SMAR-2656 HTTP `404 Not Found` and the intended active handler executes.<br><br>**Verify after step #5:**<br>• Only authorized files are returned; no blank, partial, duplicate, or unrelated record appears.<br><br>**Verify after step #6:**<br>• Re-login and refresh preserve the same authorized result set without fallback to Matrix M1.<br><br>**Verify after step #7:**<br>• UI, API, and backend results match and no Matrix M1 request is observed. | Retained as distinct active-consumer regression. |
| SMAR-2656-G3-02 | High | Approved supported alarm routes in SHM and SHM ALM | POST | Verify supported alarm requests remain operational in both services | • Active alarm read/action requests are confirmed outside Matrix M1.<br>• Equivalent synthetic alarms and supported authentication are available.<br>• State, history, queue, notification, and trace evidence are accessible where applicable. | 1. Select and execute one supported alarm read.<br>2. Record response and returned alarm data.<br>3. Record baseline for a supported state-changing alarm action in SHM.<br>4. Execute the supported SHM action.<br>5. Review response, handler trace, state, history, and applicable downstream evidence.<br>6. Execute the applicable supported control in SHM ALM.<br>7. Review the equivalent evidence.<br>8. Compare service results. | **Verify after step #1:**<br>• The supported read is not rejected with SMAR-2656 HTTP `404 Not Found`; authorized alarm data is returned.<br><br>**Verify after step #4:**<br>• The supported SHM handler executes and the approved domain outcome is returned.<br><br>**Verify after step #5:**<br>• Alarm state/history and applicable downstream processing change once according to the supported request contract.<br><br>**Verify after steps #6–7:**<br>• SHM ALM retains its supported handler and produces its approved outcome once.<br><br>**Verify after step #8:**<br>• Deprecating Escalate and Take does not block unrelated alarm handlers or create duplicate cross-service processing. | Use isolated alarms and controlled recipients; preserve audit evidence during cleanup. |
| SMAR-2656-G3-03 | High | Selected approved active legacy routes | POST | Verify supported requests retain domain validation instead of deprecated-request rejection | • At least one active OAuth request and one active alarm request are selected.<br>• Valid supported authentication is available.<br>• Each request has one syntactically valid business-error payload.<br>• Handler traces and state evidence are accessible. | 1. Select an active supported request.<br>2. Prepare one semantically invalid payload using its documented contract.<br>3. Send it with valid supported authentication and a correlation ID.<br>4. Record the response and trace.<br>5. Review state for partial mutation.<br>6. Repeat for each selected category. | **Verify after step #3:**<br>• The request is not rejected using the SMAR-2656 HTTP `404 Not Found` deprecation contract.<br>• The documented active business validation status and error schema are returned.<br><br>**Verify after step #4:**<br>• The intended active handler or business validator is reached without exposing sensitive internals.<br><br>**Verify after step #5:**<br>• Invalid data creates no partial state.<br><br>**Verify after step #6:**<br>• Each selected category distinguishes active business validation from deprecated dispatch rejection. | Exact domain statuses come from each active endpoint contract, not SMAR-2656. |
| SMAR-2656-G3-04 | High | Current supported application workflow routes | POST | Verify supported applications complete representative workflows without Matrix M1 requests | • Current supported CP Web, Portal, Mobile, Carer App, and IPD builds are available where applicable.<br>• Representative workflows and expected outcomes are documented.<br>• Network or backend request-type telemetry is accessible. | 1. Execute a representative CP Web or Portal community/client-file workflow.<br>2. Execute the Portal suspension workflow owned by its current route.<br>3. Execute Carer App client/file refresh.<br>4. Execute a supported Mobile or IPD device/calendar workflow.<br>5. Execute a supported alarm workflow.<br>6. Execute a supported notification registration or refresh workflow.<br>7. Capture class, route, authentication context, status, and business result for every call.<br>8. Refresh or reopen each affected workflow and compare state. | **Verify after steps #1–6:**<br>• Each workflow follows its current approved outcome and no unexpected SMAR-2656 HTTP `404 Not Found`, blank data, stuck loading, partial state, or retry loop occurs.<br><br>**Verify after step #7:**<br>• No call uses a Matrix M1 request class.<br>• Every supported call maps to an approved active route and authentication context.<br>• Any Matrix M1 call is recorded as a release-blocking dependency rather than passed.<br><br>**Verify after step #8:**<br>• UI, API, and backend state remain consistent after refresh/reopen. | Targeted current-client regression; not exhaustive acceptance for related remediation tickets. |
| SMAR-2656-G3-05 | Medium | Representative supported SHM and SHM ALM control routes | POST | Verify supported requests remain available after restart and across deployed instances | • Multiple STAG instances or a controlled restart/redeploy is available.<br>• Supported SHM and SHM ALM controls are selected.<br>• Build versions, routing, handler traces, state, and integration evidence are accessible. | 1. Record deployed instance and build versions.<br>2. Execute each supported control and record its baseline result.<br>3. Restart, recycle, or redeploy the applicable STAG service.<br>4. Route controls across each available instance.<br>5. Re-execute every supported control.<br>6. Compare responses, handler traces, data, and applicable downstream evidence across instances.<br>7. Review deployment state for mixed versions. | **Verify after step #2:**<br>• Each supported control is not rejected with SMAR-2656 HTTP `404 Not Found` and follows its approved contract.<br><br>**Verify after step #5:**<br>• Supported handler registrations remain available after recovery on every instance.<br><br>**Verify after step #6:**<br>• Responses, data, and applicable downstream outcomes are consistent with baseline and occur once.<br><br>**Verify after step #7:**<br>• No mixed-version instance incorrectly classifies an active request as deprecated; authentication and authorization remain unchanged. | Operations-supported restart and per-instance routing evidence are required. |

### Deferred Scenarios

- Exact accepted status codes and payloads remain governed by each supported endpoint's existing contract.

### Requires Test Instrumentation

- Active request mapping, client network/request-type telemetry, handler traces, applicable state/history evidence, deployed instance identifiers, and controlled restart capability.

### Test Data and Cleanup

- Use synthetic STAG users, files, devices, alarms, and controlled recipients. Revert disposable state while retaining required audit and trace evidence.

## Evidence Reviewed

- Current Jira SMAR-2656 description and exact 404 addition, review comments, history, and Scoped status.
- Approved Confluence page 2609119233 current as of 2026-07-29.
- HMAC remediation inventory page 2573598721.
- Related SMAR-2638 and SMAR-2658 Second Brain requirements and cases.
- Exact user approval phrase dated 2026-07-29.

## Could Not Verify

- Exact route/method/body mapping for all Matrix M1 requests.
- Exact standard 404 response body fields.
- Runtime dispatch traces, handler-entry evidence, deployed build versions, active-consumer telemetry, and per-instance restart evidence.
- Audited inventory of additional previously deprecated request classes.

## QA Assumptions

- Current Jira Expected Behaviour and the exact approved Confluence suite supersede the older unauthorized-response developer suggestion and earlier active-ticket working decisions.
- Cases are stored as Approved and remain partially dependent on developer/operations-supported instrumentation.
