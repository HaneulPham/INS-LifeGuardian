# Run Playwright API Automation Command

```text
run API automation for <case ID, ticket ID, tag, smoke, regression, or all>

Resolve the narrowest target, validate the non-production environment, run the exact command, and report case IDs, passed/failed/flaky/skipped counts, report path, and blockers. Never claim execution when access or credentials are unavailable.
```
