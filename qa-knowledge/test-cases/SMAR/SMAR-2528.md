# SMAR-2528 Test Cases

## Test Case Coverage Summary

| Group | Coverage Objective | Stored Executable Cases | Deferred Source Cases | Priority Focus | Status |
|---|---|---:|---:|---|---|
| G1 | CP Web Create Work Order | 4 | 4 | High / Medium | Partially Covered — UI invalid-input behavior pending |
| G2 | CP Web Update Work Order | 5 | 5 | High / Medium | Partially Covered — UI invalid-input behavior pending |
| G3 | CP Web Update Invoice | 5 | 5 | High / Medium | Partially Covered — UI invalid-input behavior pending |
| G4 | API Update Work Order | 5 | 4 | High | Partially Covered — successful response and route confirmation pending |
| G5 | API Create Work Order | 4 | 4 | High | Partially Covered — successful response and route confirmation pending |
| G6 | API Update Invoice | 5 | 4 | High | Partially Covered — successful response and route confirmation pending |

## Approved Case Ledger

| Group | Source IDs | Stored | Deferred | Last Review |
|---|---|---:|---:|---|
| G1 | SMAR-2528-G1-01 to G1-08 | 4 | 4 | Confluence page 2628780034 v10; normalized 2026-08-10 |
| G2 | SMAR-2528-G2-01 to G2-10 | 5 | 5 | Confluence page 2628780034 v10; normalized 2026-08-10 |
| G3 | SMAR-2528-G3-01 to G3-10 | 5 | 5 | Confluence page 2628780034 v10; normalized 2026-08-10 |
| G4 | SMAR-2528-G4-01 to G4-09 | 5 | 4 | Confluence page 2628780034 v10; normalized 2026-08-10 |
| G5 | SMAR-2528-G5-01 to G5-08 | 4 | 4 | Confluence page 2628780034 v10; normalized 2026-08-10 |
| G6 | SMAR-2528-G6-01 to G6-09 | 5 | 4 | Confluence page 2628780034 v10; normalized 2026-08-10 |

## Duplicate Review

- Existing related test cases checked: SMAR-2633 DVA invoice cases and current Second Brain indexes.
- Duplicate coverage found: No direct duplicate.
- Related overlap: SMAR-2633 uses Presc On for invoice-date calculation and export, but does not verify SMAR-2528 invalid-value validation.
- Decision: Retain ticket-specific validation and add targeted DVA billing regression impact.
- Unique verification retained: Work Order create/update, Invoice update, exact invalid list, whitespace distinction, trimming, API 400 contract, atomic rejection, and historical correction.

## Group 1 — CP Web Create Work Order

| TC ID | Priority | Test Area | Title | Preconditions | Test Steps | Expected Result | Expected Integration | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2528-G1-04 | Medium | Work Order Creation — Optional Prior Approval Number | Verify Create Work Order accepts an empty Prior Approval Number | • Authorized Work Order creator.<br>• Synthetic client file and valid required Work Order data.<br>• Prior Approval Number is optional. | 1. Log in to CP Web.<br>2. Navigate to **Work Orders → Work Order Management**.<br>3. Select the action to create a Work Order.<br>4. Complete all required fields with synthetic valid data.<br>5. Leave **Prior Approval Number** empty.<br>6. Select **Save/Create**.<br>7. Locate and open the created Work Order. | **Verify after step #6:**<br>• One Work Order is created and no Prior Approval Number validation error appears.<br><br>**Verify after step #7:**<br>• Prior Approval Number remains empty.<br>• Other entered values persist unchanged. | **Verify after step #6:**<br>• One create request is accepted and one Work Order is persisted.<br><br>**Verify after step #7:**<br>• Retrieval shows an empty Prior Approval Number and no duplicate Work Order. | R3, R5, R6; use isolated non-production data and remove only the disposable created record. |
| SMAR-2528-G1-05 | High | Work Order Creation — Prior Approval Trimming | Verify Create Work Order trims a valid Prior Approval Number before persistence | • Authorized Work Order creator.<br>• Synthetic valid Work Order data.<br>• Test value: ` PA-12345 `. | 1. Open CP Web Work Order Management.<br>2. Start a new Work Order.<br>3. Complete required fields.<br>4. Enter ` PA-12345 ` in **Prior Approval Number**.<br>5. Select **Save/Create**.<br>6. Locate and reopen the created Work Order. | **Verify after step #5:**<br>• One Work Order is created without an invalid-value error.<br><br>**Verify after step #6:**<br>• Prior Approval Number is `PA-12345`.<br>• No leading or trailing whitespace remains and internal characters are unchanged. | **Verify after step #5:**<br>• The accepted create persists one normalized Work Order.<br><br>**Verify after step #6:**<br>• Retrieval returns `PA-12345` and no duplicate record. | R4–R6; remove only the disposable test Work Order. |
| SMAR-2528-G1-06 | Medium | Work Order Creation — Exact-Match Boundary | Verify Create Work Order accepts values containing prohibited-text substrings | • Authorized Work Order creator.<br>• Test values: `NAT12345`, `NONE-458`, `PA-N/A-123`, `ANALYTICS-01`.<br>• Each dataset uses unique synthetic Work Order data. | 1. Open CP Web Work Order Management.<br>2. Start a new Work Order.<br>3. Complete required fields.<br>4. Enter the first test value.<br>5. Select **Save/Create**.<br>6. Reopen the created Work Order.<br>7. Repeat steps 2–6 for each remaining value. | **Verify after step #5:**<br>• One Work Order is created for the dataset and no invalid-value error appears.<br><br>**Verify after step #6:**<br>• The complete value persists after supported trimming.<br><br>**Verify after step #7:**<br>• Every non-exact substring value is accepted. | **Verify after step #5:**<br>• One create is persisted for the current dataset.<br><br>**Verify after step #7:**<br>• Retrieval shows one Work Order per unique dataset with no duplicate records. | R1, R4, R8; clean up only disposable test Work Orders. |
| SMAR-2528-G1-07 | Medium | Work Order Creation — Free-Text Regression | Verify Create Work Order retains varied valid Prior Approval Number formats | • Authorized Work Order creator.<br>• Valid values: `123456`, `PA98765`, `ABC-123-XY`, `12/34567`, `PaRef2026`.<br>• Unique synthetic Work Order data exists for each value. | 1. Open CP Web Work Order Management.<br>2. Start a new Work Order.<br>3. Complete required fields.<br>4. Enter the first valid format.<br>5. Select **Save/Create**.<br>6. Reopen the created Work Order.<br>7. Repeat steps 2–6 for the remaining formats. | **Verify after step #5:**<br>• Creation is accepted without an undocumented format error.<br><br>**Verify after step #6:**<br>• The valid value persists exactly after supported trimming.<br><br>**Verify after step #7:**<br>• All listed free-text formats remain accepted. | **Verify after step #5:**<br>• One Work Order is persisted for the dataset.<br><br>**Verify after step #7:**<br>• Retrieval shows the expected values and no duplicate Work Orders. | R4, R8; clean up only disposable test Work Orders. |

### Deferred Scenarios

- SMAR-2528-G1-01, G1-02, G1-03, and G1-08 — defer the executable UI error/retention expectation until Jira comment 44529 confirms error display versus field clearing and the exact Work Order message.

### Test Data and Cleanup

- Use unique synthetic Work Orders; remove only disposable successfully created test records and preserve required audit evidence.

## Group 2 — CP Web Update Work Order

| TC ID | Priority | Test Area | Title | Preconditions | Test Steps | Expected Result | Expected Integration | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2528-G2-04 | Medium | Work Order Update — Optional Prior Approval Number | Verify Update Work Order allows an existing Prior Approval Number to be cleared | • Authorized Work Order editor.<br>• Existing synthetic Work Order contains `PA-12345`.<br>• An unrelated editable field has a recorded baseline value. | 1. Log in to CP Web.<br>2. Navigate to **Work Orders → Work Order Management**.<br>3. Locate and open the prepared Work Order.<br>4. Confirm Prior Approval Number is `PA-12345` and record the baseline state.<br>5. Clear Prior Approval Number completely.<br>6. Select **Save/Update**.<br>7. Reopen the Work Order. | **Verify after step #6:**<br>• The update is accepted without an invalid-value error.<br><br>**Verify after step #7:**<br>• Prior Approval Number remains empty.<br>• Unrelated Work Order fields remain unchanged. | **Verify after step #6:**<br>• Exactly one update is persisted.<br><br>**Verify after step #7:**<br>• Retrieval returns an empty Prior Approval Number and no duplicate or delayed update. | R3, R5, R6; preserve baseline evidence and unrelated data. |
| SMAR-2528-G2-05 | High | Work Order Update — Prior Approval Trimming | Verify Update Work Order trims a valid Prior Approval Number before persistence | • Authorized Work Order editor.<br>• Existing synthetic Work Order contains `OLD-123`.<br>• New value: ` PA-67890 `.<br>• An unrelated editable field has a recorded baseline value. | 1. Open **Work Orders → Work Order Management**.<br>2. Locate and open the prepared Work Order.<br>3. Enter ` PA-67890 ` in Prior Approval Number.<br>4. Select **Save/Update**.<br>5. Reopen the Work Order. | **Verify after step #4:**<br>• The update is accepted without an invalid-value error.<br><br>**Verify after step #5:**<br>• Prior Approval Number is `PA-67890` without leading or trailing whitespace.<br>• Internal characters and unrelated fields remain unchanged. | **Verify after step #4:**<br>• Exactly one normalized update is persisted.<br><br>**Verify after step #5:**<br>• Retrieval returns `PA-67890` and no duplicate update. | R4–R6; use synthetic records and preserve unrelated state. |
| SMAR-2528-G2-06 | Medium | Work Order Update — Exact-Match Boundary | Verify Update Work Order accepts values containing prohibited-text substrings | • Authorized Work Order editor.<br>• Existing synthetic Work Order is editable.<br>• Values: `NAT12345`, `NONE-458`, `PA-N/A-123`, `ANALYTICS-01`.<br>• An unrelated editable field has a recorded baseline value. | 1. Open **Work Orders → Work Order Management**.<br>2. Locate and open the prepared Work Order.<br>3. Enter the first test value.<br>4. Select **Save/Update**.<br>5. Reopen the Work Order.<br>6. Repeat steps 2–5 for the remaining values. | **Verify after step #4:**<br>• The update is accepted because the normalized value is not an exact prohibited value.<br><br>**Verify after step #5:**<br>• The full value persists after supported trimming.<br><br>**Verify after step #6:**<br>• Every substring value is accepted and unrelated state remains unchanged. | **Verify after step #4:**<br>• Exactly one update is persisted for the dataset.<br><br>**Verify after step #6:**<br>• Retrieval shows the final accepted value and no duplicate or delayed updates. | R1, R4, R8; preserve baseline and unrelated data. |
| SMAR-2528-G2-07 | Medium | Work Order Update — Free-Text Regression | Verify Update Work Order retains varied valid Prior Approval Number formats | • Authorized Work Order editor.<br>• Existing synthetic Work Order is editable.<br>• Values: `123456`, `PA98765`, `ABC-123-XY`, `12/34567`, `PaRef2026`.<br>• An unrelated editable field has a recorded baseline value. | 1. Open **Work Orders → Work Order Management**.<br>2. Locate and open the prepared Work Order.<br>3. Enter the first valid format.<br>4. Select **Save/Update**.<br>5. Reopen the Work Order.<br>6. Repeat steps 2–5 for the remaining formats. | **Verify after step #4:**<br>• The update is accepted without an undocumented format restriction.<br><br>**Verify after step #5:**<br>• The complete value persists after supported trimming.<br><br>**Verify after step #6:**<br>• All listed formats remain accepted and unrelated state remains unchanged. | **Verify after step #4:**<br>• Exactly one update is persisted for the dataset.<br><br>**Verify after step #6:**<br>• Retrieval shows the accepted value and no duplicate update. | R4, R8; preserve baseline and unrelated data. |
| SMAR-2528-G2-09 | High | Work Order Update — Historical Data Correction | Verify correcting an existing invalid Work Order Prior Approval Number allows the update | • Authorized Work Order editor.<br>• Historical synthetic Work Order contains `None`.<br>• Replacement value: ` PA-2026-001 `.<br>• An unrelated editable field has a recorded baseline value. | 1. Open **Work Orders → Work Order Management**.<br>2. Locate and open the historical Work Order.<br>3. Confirm Prior Approval Number is `None` and record unrelated baseline state.<br>4. Replace it with ` PA-2026-001 `.<br>5. Apply one valid unrelated field change.<br>6. Select **Save/Update**.<br>7. Reopen the Work Order. | **Verify after step #6:**<br>• The corrected update is accepted.<br><br>**Verify after step #7:**<br>• Prior Approval Number is `PA-2026-001`.<br>• The intended unrelated change persists and the historical invalid value is absent.<br>• No other Work Order field changes. | **Verify after step #6:**<br>• Exactly one corrected update is persisted.<br><br>**Verify after step #7:**<br>• Retrieval returns the normalized replacement and no duplicate or stale update. | R4–R6, R10; use synthetic historical data and preserve unrelated state. |

### Deferred Scenarios

- SMAR-2528-G2-01, G2-02, G2-03, G2-08, and G2-10 — defer the executable UI error/retention/recovery expectation until Jira comment 44529 is resolved and the exact message is confirmed.

### Test Data and Cleanup

- Use synthetic editable and historical Work Orders; record baselines and leave unrelated data unchanged.

## Group 3 — CP Web Update Invoice

| TC ID | Priority | Test Area | Title | Preconditions | Test Steps | Expected Result | Expected Integration | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2528-G3-04 | Medium | Invoice Update — Optional Prior Approval Number | Verify Update Invoice allows an existing Prior Approval Number to be cleared | • Authorized Invoice editor.<br>• Existing synthetic Invoice contains `PA-12345`.<br>• Invoice status, totals, payment state, and export state are recorded. | 1. Log in to CP Web.<br>2. Navigate to the CP Web invoice-management area.<br>3. Locate and open the prepared Invoice.<br>4. Confirm Prior Approval Number is `PA-12345` and record the baseline state.<br>5. Clear Prior Approval Number completely.<br>6. Select **Save/Update**.<br>7. Reopen the Invoice. | **Verify after step #6:**<br>• The update is accepted without an invalid-value error.<br><br>**Verify after step #7:**<br>• Prior Approval Number remains empty.<br>• Invoice status, financial values, and unrelated fields remain unchanged. | **Verify after step #6:**<br>• Exactly one update is persisted.<br><br>**Verify after step #7:**<br>• Retrieval returns an empty Prior Approval Number and no duplicate or delayed update. | R3, R5, R6; preserve baseline evidence and unrelated data. |
| SMAR-2528-G3-05 | High | Invoice Update — Prior Approval Trimming | Verify Update Invoice trims a valid Prior Approval Number before persistence | • Authorized Invoice editor.<br>• Existing synthetic Invoice contains `OLD-123`.<br>• New value: ` PA-67890 `.<br>• Invoice status, totals, payment state, and export state are recorded. | 1. Open the CP Web invoice-management area.<br>2. Locate and open the prepared Invoice.<br>3. Enter ` PA-67890 ` in Prior Approval Number.<br>4. Select **Save/Update**.<br>5. Reopen the Invoice. | **Verify after step #4:**<br>• The update is accepted without an invalid-value error.<br><br>**Verify after step #5:**<br>• Prior Approval Number is `PA-67890` without leading or trailing whitespace.<br>• Internal characters and invoice financial/status values remain unchanged. | **Verify after step #4:**<br>• Exactly one normalized update is persisted.<br><br>**Verify after step #5:**<br>• Retrieval returns `PA-67890` and no duplicate update. | R4–R6; use synthetic records and preserve unrelated state. |
| SMAR-2528-G3-06 | Medium | Invoice Update — Exact-Match Boundary | Verify Update Invoice accepts values containing prohibited-text substrings | • Authorized Invoice editor.<br>• Existing synthetic Invoice is editable.<br>• Values: `NAT12345`, `NONE-458`, `PA-N/A-123`, `ANALYTICS-01`.<br>• Invoice status, totals, payment state, and export state are recorded. | 1. Open the CP Web invoice-management area.<br>2. Locate and open the prepared Invoice.<br>3. Enter the first test value.<br>4. Select **Save/Update**.<br>5. Reopen the Invoice.<br>6. Repeat steps 2–5 for the remaining values. | **Verify after step #4:**<br>• The update is accepted because the normalized value is not an exact prohibited value.<br><br>**Verify after step #5:**<br>• The full value persists after supported trimming.<br><br>**Verify after step #6:**<br>• Every substring value is accepted and unrelated state remains unchanged. | **Verify after step #4:**<br>• Exactly one update is persisted for the dataset.<br><br>**Verify after step #6:**<br>• Retrieval shows the final accepted value and no duplicate or delayed updates. | R1, R4, R8; preserve baseline and unrelated data. |
| SMAR-2528-G3-07 | Medium | Invoice Update — Free-Text Regression | Verify Update Invoice retains varied valid Prior Approval Number formats | • Authorized Invoice editor.<br>• Existing synthetic Invoice is editable.<br>• Values: `123456`, `PA98765`, `ABC-123-XY`, `12/34567`, `PaRef2026`.<br>• Invoice status, totals, payment state, and export state are recorded. | 1. Open the CP Web invoice-management area.<br>2. Locate and open the prepared Invoice.<br>3. Enter the first valid format.<br>4. Select **Save/Update**.<br>5. Reopen the Invoice.<br>6. Repeat steps 2–5 for the remaining formats. | **Verify after step #4:**<br>• The update is accepted without an undocumented format restriction.<br><br>**Verify after step #5:**<br>• The complete value persists after supported trimming.<br><br>**Verify after step #6:**<br>• All listed formats remain accepted and unrelated state remains unchanged. | **Verify after step #4:**<br>• Exactly one update is persisted for the dataset.<br><br>**Verify after step #6:**<br>• Retrieval shows the accepted value and no duplicate update. | R4, R8; preserve baseline and unrelated data. |
| SMAR-2528-G3-09 | High | Invoice Update — Historical Data Correction | Verify correcting an existing invalid Invoice Prior Approval Number allows the update | • Authorized Invoice editor.<br>• Historical synthetic Invoice contains `None`.<br>• Replacement value: ` PA-2026-001 `.<br>• Invoice status, totals, payment state, and export state are recorded. | 1. Open the CP Web invoice-management area.<br>2. Locate and open the historical Invoice.<br>3. Confirm Prior Approval Number is `None` and record unrelated baseline state.<br>4. Replace it with ` PA-2026-001 `.<br>5. Apply one valid unrelated field change.<br>6. Select **Save/Update**.<br>7. Reopen the Invoice. | **Verify after step #6:**<br>• The corrected update is accepted.<br><br>**Verify after step #7:**<br>• Prior Approval Number is `PA-2026-001`.<br>• The intended unrelated change persists and the historical invalid value is absent.<br>• Invoice financial and export state remains unchanged. | **Verify after step #6:**<br>• Exactly one corrected update is persisted.<br><br>**Verify after step #7:**<br>• Retrieval returns the normalized replacement and no duplicate or stale update. | R4–R6, R10; use synthetic historical data and preserve unrelated state. |

### Deferred Scenarios

- SMAR-2528-G3-01, G3-02, G3-03, G3-08, and G3-10 — defer the executable UI error/retention/recovery expectation until Jira comment 44529 is resolved and the exact Invoice message is confirmed.

### Test Data and Cleanup

- Use synthetic editable and historical Invoices; record financial, payment, status, and export baselines and preserve them.

## Group 4 — API Update Work Order

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2528-G4-01 | High | /lg/workorder/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify Update Work Order API rejects exact prohibited values case-insensitively | • Existing synthetic Work Order with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Parameterize an otherwise valid update with `N/A`, `n/a`, `N/a`, `NA`, `na`, `Na`, `None`, `NONE`, and `none`. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• Existing Work Order Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G4-02 | High | /lg/workorder/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify Update Work Order API rejects prohibited values surrounded by whitespace | • Existing synthetic Work Order with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Parameterize with ` N/A `, ` NA`, `n/a `, tab-prefixed `None`, line-break-suffixed `NA`, and non-breaking-space-surrounded `None`. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• Existing Work Order Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G4-03 | High | /lg/workorder/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify Update Work Order API rejects whitespace-only Prior Approval Numbers | • Existing synthetic Work Order with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Send separate requests containing one space, multiple spaces, tab only, line break only, non-breaking spaces only, and mixed whitespace. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• Existing Work Order Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G4-07 | High | /lg/workorder/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify rejected Prior Approval Number prevents partial Work Order API updates | • Existing synthetic Work Order with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Send `N/A` together with a valid change to an unrelated editable field. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• Existing Work Order Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update.<br>• No partial field change is persisted. | R1, R2, R5–R7, R9; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G4-08 | High | /lg/workorder/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify a historical invalid Work Order rejects unrelated API updates until corrected | • Existing synthetic Work Order with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Retain historical Prior Approval Number `N/A` and change an unrelated editable field. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• Existing Work Order Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update.<br>• The historical invalid value remains available for correction; the unrelated requested change does not persist.<br>• No partial field change is persisted. | R1, R2, R5–R7, R10, R9; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |

### Deferred Scenarios

- SMAR-2528-G4-04, G4-05, G4-06, and G4-09 — positive API cases require the successful HTTP status and response schema plus confirmation of A1.

## Group 5 — API Create Work Order

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2528-G5-01 | High | /lg/workorder/v2 | POST | Verify Create Work Order API rejects exact prohibited values case-insensitively | • Valid synthetic create payload and unique reference.<br>• Valid authentication and required headers. | Send otherwise valid unique create requests parameterized with `N/A`, `n/a`, `N/a`, `NA`, `na`, `Na`, `None`, `NONE`, and `none`. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• No Work Order, child data, duplicate, or delayed record is persisted. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G5-02 | High | /lg/workorder/v2 | POST | Verify Create Work Order API rejects prohibited values surrounded by whitespace | • Valid synthetic create payload and unique reference.<br>• Valid authentication and required headers. | Send unique create requests using ` N/A `, ` NA`, `n/a `, tab-prefixed `None`, line-break-suffixed `NA`, and non-breaking-space-surrounded `None`. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• No Work Order, child data, duplicate, or delayed record is persisted. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G5-03 | High | /lg/workorder/v2 | POST | Verify Create Work Order API rejects whitespace-only Prior Approval Numbers | • Valid synthetic create payload and unique reference.<br>• Valid authentication and required headers. | Send separate unique requests containing one space, multiple spaces, tab only, line break only, non-breaking spaces only, and mixed whitespace. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• No Work Order, child data, duplicate, or delayed record is persisted. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G5-08 | High | /lg/workorder/v2 | POST | Verify rejected Prior Approval Number prevents partial or duplicate Work Order creation | • Valid synthetic create payload and unique reference.<br>• Valid authentication and required headers. | Send the same otherwise valid unique create request containing `N/A` twice, then retrieve by the unique reference. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Work Order object or identifier is returned.<br>• No Work Order, child data, duplicate, or delayed record is persisted.<br>• No partial field change is persisted. | R1, R2, R5–R7, R9; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |

### Deferred Scenarios

- SMAR-2528-G5-04, G5-05, G5-06, and G5-07 — positive API cases require the successful HTTP status and response schema plus confirmation of A2.

## Group 6 — API Update Invoice

| TC ID | Priority | API Endpoint | Method | Title | Preconditions | Request Data | Expected Response | Notes |
|---|---|---|---|---|---|---|---|---|
| SMAR-2528-G6-01 | High | /lg/invoice/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify Update Invoice API rejects exact prohibited values case-insensitively | • Existing synthetic Invoice with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Parameterize an otherwise valid update with `N/A`, `n/a`, `N/a`, `NA`, `na`, `Na`, `None`, `NONE`, and `none`. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Invoice object or identifier is returned.<br>• Existing Invoice Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G6-02 | High | /lg/invoice/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify Update Invoice API rejects prohibited values surrounded by whitespace | • Existing synthetic Invoice with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Parameterize with ` N/A `, ` NA`, `n/a `, tab-prefixed `None`, line-break-suffixed `NA`, and non-breaking-space-surrounded `None`. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Invoice object or identifier is returned.<br>• Existing Invoice Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G6-03 | High | /lg/invoice/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify Update Invoice API rejects whitespace-only Prior Approval Numbers | • Existing synthetic Invoice with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Send separate requests containing one space, multiple spaces, tab only, line break only, non-breaking spaces only, and mixed whitespace. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Invoice object or identifier is returned.<br>• Existing Invoice Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update. | R1, R2, R5–R7; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G6-07 | High | /lg/invoice/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify rejected Prior Approval Number prevents partial Invoice API updates | • Existing synthetic Invoice with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Send `N/A` together with a valid unrelated field change after recording invoice status, totals, payment state, and export state. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Invoice object or identifier is returned.<br>• Existing Invoice Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update.<br>• No partial field change is persisted. | R1, R2, R5–R7, R9; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |
| SMAR-2528-G6-08 | High | /lg/invoice/v2/{fileUuid}?CreAt={CreAt} | PUT | Verify a historical invalid Invoice rejects unrelated API updates until corrected | • Existing synthetic Invoice with recorded Prior Approval Number and unrelated baseline state.<br>• Valid authentication and required headers. | Retain historical Prior Approval Number `N/A` and change an unrelated editable field after recording invoice financial and export state. | HTTP **400 Bad Request**.<br>• `Error` is `bad_request`.<br>• `ErrorDescription` is `'Dva Presc On' must be a valid value or blank`.<br>• No accepted Invoice object or identifier is returned.<br>• Existing Invoice Prior Approval Number and unrelated fields remain unchanged.<br>• Repeating the rejected request creates no duplicate or delayed update.<br>• The historical invalid value remains available for correction; the unrelated requested change does not persist.<br>• No partial field change is persisted. | R1, R2, R5–R7, R10, R9; endpoint/method uses the applicable A1–A3 assumption and must be confirmed before execution. |

### Deferred Scenarios

- SMAR-2528-G6-04, G6-05, G6-06, and G6-09 — positive API cases require the successful HTTP status and response schema plus confirmation of A3.

## Review Classification

| Classification | Cases | Reason |
|---|---:|---|
| Add | 28 | Evidence-supported primary outcomes can be stored as executable rows after safe formatting normalization |
| Update | 28 | Added numbered steps, observable integration evidence, privacy-safe data, traceability notes, and cleanup without changing supported behavior |
| Merge | 0 | Separate create/update/entity/platform outcomes require distinct setup and evidence |
| Remove | 0 | No source ID or unique scenario was deleted |
| Retain as Deferred | 26 | UI behavior/message conflict or missing successful API response contract prevents a final executable outcome |
| Could Not Verify | 12 | Positive API response status/schema and deployed A1–A3 routes/methods were not supplied |

## Planned-versus-Actual Reconciliation

| Group | Source Count | Stored Executable Count | Deferred Count | Reason | Coverage State |
|---|---:|---:|---:|---|---|
| G1 | 8 | 4 | 4 | Invalid-input UI presentation is unresolved | Partially Covered |
| G2 | 10 | 5 | 5 | Invalid-input UI presentation and recovery are unresolved | Partially Covered |
| G3 | 10 | 5 | 5 | Invalid-input UI presentation and recovery are unresolved | Partially Covered |
| G4 | 9 | 5 | 4 | Positive API contract is incomplete | Partially Covered |
| G5 | 8 | 4 | 4 | Positive API contract is incomplete | Partially Covered |
| G6 | 9 | 5 | 4 | Positive API contract is incomplete | Partially Covered |
| **Total** | **54** | **28** | **26** | Current evidence supports partial executable storage | **Partially Covered** |

